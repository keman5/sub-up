# 2026-07 Fork 维护明细

本文件记录 2026-07 的非上游本地改动明细。主入口仍是 `docs/FORK_MAINTENANCE_CN.md`；这里用于承接较长的每日补丁说明、验证命令和同步官方后的复查点，避免主文档持续膨胀。

## 本地补丁记录表

| 日期 | 问题 | 本地修复 | 验证 | 后续同步复查 |
| --- | --- | --- | --- | --- |
| 2026-07-03 | 管理后台表格行操作里的“更多”菜单使用固定高度估算定位；账号管理菜单项增多后，靠近屏幕底部的行会把菜单渲染到视口下方，导致底部操作无法点击。用户管理等同类表格行菜单也存在同样风险。 | 新增公共定位工具 `frontend/src/utils/floatingMenuPosition.ts`，先按触发按钮和视口给出初始位置，再在菜单 Teleport 到 body 后读取真实 DOM 尺寸并夹紧到视口内；账号管理 `AccountActionMenu` 和用户管理行操作菜单都改用该逻辑，并设置 `max-height` + 内部滚动兜底，短屏或菜单项变化时不会撑出屏幕。 | `pnpm --dir frontend exec vitest run src/utils/__tests__/floatingMenuPosition.spec.ts src/components/admin/account/__tests__/AccountActionMenu.spark_shadow.spec.ts src/views/admin/__tests__/AccountsView.sparkShadow.spec.ts src/views/admin/__tests__/UsersView.spec.ts`；`pnpm --dir frontend run typecheck`；`pnpm --dir frontend run build`；`git diff --check`。 | 同步官方后搜索 `floatingMenuPosition`、`clampFloatingMenuPosition`、`getActionMenuPosition`、`AccountActionMenu`、`actionMenuContentRef`、`action-menu-content`。如果官方重构表格操作菜单或改用统一浮层库，需要继续确认账号管理、用户管理等表格底部行打开“更多”时不会超出屏幕；菜单项继续增减时不得恢复固定 `menuHeight=240` 作为唯一边界判断。 |
| 2026-07-05 | 客户遇到请求失败时只能把错误贴给管理员，无法自行判断是端点/模型/API Key 配置问题、额度/限流问题，还是上游账号池/代理/后端服务问题；如果直接接入 AI，又可能被当作通用聊天消耗大量 token。 | 新增登录态右下角 `故障排查助手`，前端仅提交当前粘贴的报错文本；后端新增 `POST /api/v1/troubleshooting/analyze`，先做“只允许排查请求失败”的用途限制和单用户 `5 分钟 10 次 / 每日 50 次` 限流；后端优先查询系统请求详情和当前 OpenAI 账号池状态，能确认时直接输出 `排查结果`，不再罗列多个可能原因；诊断输出按结论动态组合，不使用固定四段模板；如果判断暂不需要管理员处理，不输出“是否需要联系管理员/暂时不需要联系管理员/建议提供给管理员的信息”等冗余段落，AI 返回这类模板残留也会被过滤；只有证据不足时才最多尝试 3 个可调度 OpenAI 账号做短输出 AI 诊断。OAuth internal API 请求不带 `max_output_tokens`，AI 超时调整为 30 秒，并优先使用 `gpt-5.3-codex-spark`；未展开按钮和展开面板都可拖动并保存位置；需要管理员处理的诊断可点击 `通知管理员`，后端只向现有账号额度通知邮箱中的已验证、未禁用地址发送邮件，并按同一报错跨用户 10 分钟合并去重；重复提醒时不再发邮件，返回“管理员已收到，正在处理，请等待 5 分钟后重试。”；通知接口同样校验报错用途并复用故障排查用户限流，避免绕过 `/analyze` 直接刷邮件。AI 不可用时返回规则诊断并提示联系管理员；深色模式下悬浮按钮改用高对比蓝色背景，避免与全局背景重合。 | `go test ./internal/service -run 'TroubleshootingAssistant|NotificationEmail|ImageGenerationPermission|ClientErrorMessage|GetSubscriptionProgress' -count=1`；`pnpm --dir frontend exec vitest run src/components/common/__tests__/TroubleshootingAssistant.spec.ts`；`go test ./cmd/server ./internal/service ./internal/handler ./internal/repository ./ent/... -count=1`；`pnpm --dir frontend run typecheck`；本地 `POST /api/v1/troubleshooting/analyze` 验证返回 `source=ai` 或证据命中时返回 `source=rules` 且不调用 AI。 | 同步官方后搜索 `TroubleshootingAssistant`、`troubleshooting/analyze`、`troubleshooting/notify-admin`、`troubleshooting.admin_notify`、`TroubleshootingAssistantService`、`SystemTroubleshootingEvidenceProvider`、`RedisTroubleshootingRateLimiter`、`OpenAITroubleshootingAIClient`。如官方新增客服/诊断助手，必须保留“用途限制、后端限流、先查系统证据、OAuth internal API 参数兼容、AI 账号不可用时联系管理员、通知管理员只走已配置通知邮箱且 10 分钟跨用户去重”的边界。 |
| 2026-07-05 | 管理员在订阅管理里给同一账号再次分配同一套餐时，后端只返回“订阅已存在/语义冲突”类错误，前端直接错误提示，管理员无法确认后继续覆盖旧订阅。 | 单用户分配订阅时，后端对需要人工确认的重复订阅返回 `SUBSCRIPTION_DUPLICATE_CONFIRMATION_REQUIRED`；前端捕获该错误并弹出确认框，说明继续后会用本次分配覆盖旧套餐周期，而不是创建第二条同账号同套餐记录。确认后带 `confirm_duplicate=true` 重试，原订阅记录保留 ID 但重置开始时间、到期时间、用量统计、分配人和备注。 | `go test ./internal/service -run 'TestAssignSubscriptionDuplicateRequiresConfirmationWhenSemanticsMismatch|TestAssignSubscriptionConfirmedDuplicateReplacesExisting' -count=1`；`go test ./internal/service -run AssignSubscription -count=1`；`pnpm --dir frontend run typecheck`；`git diff --check`。 | 同步官方后搜索 `SUBSCRIPTION_DUPLICATE_CONFIRMATION_REQUIRED`、`confirm_duplicate`、`duplicateConfirmTitle`、`assignSingleSubscriptionWithDuplicateConfirmation`。如果官方改造订阅分配/幂等逻辑，要保留“未确认不修改、确认后覆盖旧记录、不创建重复 user_id/group_id 订阅”的边界。 |
| 2026-07-05 | 后端结构化错误虽然已经带 `reason`，但 `message` 仍直接返回服务端硬编码文案；大量历史 handler 还直接返回英文 `Invalid ...` / `User not authenticated`。中英文混用时，前端全局错误提示、订阅覆盖确认、故障排查限流和故障排查正文不能稳定跟随当前界面语言。管理员订阅进度接口还会把所有 `GetSubscriptionProgress` 错误都包装成 404 `Subscription not found`，容易掩盖真实后端错误。 | `response.ErrorFrom` / `response.Error` / `response.ErrorWithDetails` 根据请求 `Accept-Language` 替换展示用 `message`，保留原有 `reason` / `metadata` 不变；首批覆盖认证、OAuth pending、订阅、故障排查、运维错误日志、Turnstile、TOTP、兑换码等结构化错误，并补常见直接 handler 英文消息的中文映射和通用 `Invalid ... ID` / `Invalid request: ...` 规则。故障排查助手从 handler 传入 `Accept-Language`，本地规则、系统证据、AI 不可用提示和 AI prompt 均支持中英文。管理员订阅进度接口改为直接 `response.ErrorFrom`，订阅不存在仍返回 `SUBSCRIPTION_NOT_FOUND`，底层非 NotFound 错误不再伪装成 404；订阅分配弹窗的非重复错误改用统一错误提取，避免 `skipGlobalErrorToast` 后只显示泛化失败文案；`Image generation is not enabled for this group` 同步补中文文案，OpenAI gateway 直接 JSON 响应也按 `Accept-Language` 输出。 | `go test -tags unit ./internal/pkg/response -run 'TestErrorFromLocalizes|TestErrorLocalizes' -count=1`；`go test ./internal/service -run 'TroubleshootingAssistant|TroubleshootingEvidence|TroubleshootingAIModel|BuildTroubleshootingAIRequestBody|ExtractTroubleshootingAIText|RedisTroubleshootingRateLimiter|GetSubscriptionProgress|ImageGenerationPermission' -count=1`；`go test ./internal/handler/admin ./internal/handler -run Test -count=1`；`pnpm --dir frontend run typecheck`；`git diff --check`。 | 同步官方后搜索 `localizedErrorMessages`、`localizedDirectErrorMessages`、`localizeErrorMessage`、`requestErrorLocale`、`ImageGenerationPermissionMessageForAcceptLanguage`、`Accept-Language`、`ErrorFrom`、`buildTroubleshootingAIPrompt` 和 `normalizeTroubleshootingLocale`。如果官方新增统一错误 i18n，需要确认仍保留：仅替换展示 message、不改变 reason/metadata、未知 reason 原样兜底；新业务错误应优先使用结构化 reason，避免继续新增直接英文字符串。 |
| 2026-07-06 | 右下角故障排查悬浮按钮占用屏幕空间，需要按钮右上角提供关闭入口，点击后自动收起到屏幕右侧，仅保留很小的恢复箭头，并兼顾深色/浅色主题可见性；展开面板自身关闭时应恢复原完整按钮，而不是直接进入缩小状态。 | 故障排查助手新增 `collapsed` 收起态：未展开的 `故障排查` 悬浮按钮右上角增加小关闭按钮，点击后隐藏完整按钮并贴右侧显示更窄的箭头恢复按钮；展开面板右上角关闭只回到原完整悬浮按钮；恢复箭头点击后回到原保存位置直接展开。收起把手不覆盖原拖拽位置，只临时固定到 `right: 0`；收起状态写入 `localStorage` 的 `sub2api.troubleshootingAssistant.collapsed`，刷新后仍保持右侧小箭头，恢复/打开/关闭面板时写回未收起。浅色主题使用白底深色箭头，深色主题使用高对比蓝色背景和边框。 | `pnpm --dir frontend exec vitest run src/components/common/__tests__/TroubleshootingAssistant.spec.ts`；`pnpm --dir frontend run typecheck`；`git diff --check`。 | 同步官方后搜索 `collapsed`、`sub2api.troubleshootingAssistant.collapsed`、`assistant-fab-close`、`assistant-restore-handle`、`收起故障排查入口`、`关闭故障排查助手`、`恢复故障排查助手`。如果官方重构悬浮入口，需要保留“悬浮按钮右上角可收起、面板关闭恢复完整按钮、收起后只露出右侧小箭头且刷新后保持收起、恢复后使用原面板位置、深浅主题都有足够对比度”的行为。 |
| 2026-07-06 | 继续排查后发现部分接口 `message` 和前台本地校验仍会显示英文：OpenAI OAuth 缺参数、定时测试计划加载/保存失败、图片上传本地校验，以及后端 OAuth/代理/运维、令牌、注册/邀请码、优惠码、退款、setup 和 dashboard/ops 固定错误。 | 继续扩展 `localizedErrorMessages` / `localizedDirectErrorMessages`，补充 auth/OAuth pending、proxy、ops、token、registration、promo/refund、usage cleanup、setup 参数、dashboard/ops settings 固定错误的中英文/中文映射；OpenAI OAuth、定时测试面板和 `ImageUpload` 改用 i18n key；前端剩余硬编码只保留内部 sentinel/control-flow error，不作为展示文案。 | `cd backend && go test ./internal/pkg/response -run 'TestErrorFromLocalizesStructuredErrorsByAcceptLanguage|TestErrorLocalizesCommonDirectHandlerMessagesByAcceptLanguage'`；`pnpm --dir frontend exec vitest run src/composables/__tests__/useOpenAIOAuth.spec.ts src/components/common/__tests__/ImageUpload.spec.ts src/components/admin/account/__tests__/ScheduledTestsPanel.spec.ts`。 | 同步官方后搜索 `missingExchangeParams`、`missingRefreshToken`、`loadPlansFailed`、`fileTooLarge`、`REFRESH_TOKEN_EXPIRED`、`PROMO_CODE_EXPIRED`、`Setup is not allowed`、`platform quota service not available`。低层幂等、内部事务、上游透传和字段级配置校验不要强行翻译成客户结论，除非对应接口确认为用户直接可见。 |
| 2026-07-07 | 账号管理里 OpenAI OAuth 账号手动查询 quota 后，`AccountUsageCell` 会刷新用量单元格，但外层账号表格行仍可能保留旧运行态/额度状态，造成列表状态与刚查询到的 upstream quota 不一致。 | `AccountUsageCell` 新增 `runtime-state-updated` 事件：主动查询 OpenAI quota 成功后，或外部 OpenAI quota query 回调完成后通知父级；`AccountsView` 收到事件后重置自动刷新缓存并执行增量刷新，让账号行状态、quota 运行态和用量单元格一起更新。 | `pnpm --dir frontend exec vitest run src/components/account/__tests__/AccountUsageCell.spec.ts`；`pnpm --dir frontend run typecheck`；`git diff --check`。 | 同步官方后搜索 `runtime-state-updated`、`handleAccountRuntimeStateUpdated`、`queryOpenAIQuota`、`loadActiveUsage`。如果官方改造账号列表刷新或用量单元格，需要继续保留“手动查询 upstream quota 后刷新外层账号行运行态”的行为，避免只更新 cell 内部缓存。 |

## 详细记录

## 2026-07-03: 表格行操作菜单底部防溢出

**本地补丁：**

- 新增 `floatingMenuPosition` 公共定位工具，将浮层位置按视口边界夹紧，并返回可用最大高度。
- 账号管理行操作 `AccountActionMenu` 在 Teleport 到 body 后读取真实菜单尺寸，重新校正 top/left，并设置 `max-height` 与 `overflow-y: auto`。
- 用户管理行操作菜单复用同一套定位逻辑，避免同类“更多”菜单在底部行打开后超出屏幕。
- 初始定位仍保留桌面端靠近点击点、移动端围绕触发按钮的原交互；本次不改业务菜单项和按钮行为。

**涉及文件：**

- `frontend/src/utils/floatingMenuPosition.ts`
- `frontend/src/utils/__tests__/floatingMenuPosition.spec.ts`
- `frontend/src/components/admin/account/AccountActionMenu.vue`
- `frontend/src/views/admin/AccountsView.vue`
- `frontend/src/views/admin/UsersView.vue`
- `docs/fork-maintenance/2026-07.md`

**验证：**

```bash
pnpm --dir frontend exec vitest run src/utils/__tests__/floatingMenuPosition.spec.ts src/components/admin/account/__tests__/AccountActionMenu.spark_shadow.spec.ts src/views/admin/__tests__/AccountsView.sparkShadow.spec.ts src/views/admin/__tests__/UsersView.spec.ts
pnpm --dir frontend run typecheck
pnpm --dir frontend run build
git diff --check
```

**同步官方后的复查：**

- 搜索 `floatingMenuPosition`、`clampFloatingMenuPosition`、`getActionMenuPosition`、`AccountActionMenu`、`actionMenuContentRef` 和 `action-menu-content`。
- 如果官方后续引入统一 Dropdown / Popover / Floating UI，优先收敛到官方浮层实现，但必须保留“按真实菜单高度校正位置”和“短屏内可滚动”的行为。
- 回归账号管理、用户管理以及后续新增的表格行“更多”菜单：在最后几行打开菜单时，底部操作项应保持在视口内可点击。

## 2026-07-05: 客户故障排查助手

**本地补丁：**

- 新增 `TroubleshootingAssistantService`，统一处理用途限制、输入截断、系统证据查询、本地规则诊断、限流和 AI 诊断兜底。
- 新增 Redis 限流器：单用户 5 分钟最多 10 次、每日最多 50 次，命中后返回 429，不进入 AI 调用。
- 新增 `SystemTroubleshootingEvidenceProvider`：优先用 request id 查询系统请求详情，并读取当前 OpenAI 账号池可用状态；如果查到明确错误日志或当前账号池已恢复，直接返回 `排查结果`，不再输出多个“可能原因”；输出按结论动态组合，不使用固定四段模板；如果判断暂不需要管理员处理，不输出“是否需要联系管理员/暂时不需要联系管理员/建议提供给管理员的信息”等冗余段落，AI 返回这类模板残留也会被后端过滤。
- 新增 OpenAI AI 诊断客户端：从可用 OpenAI 账号中最多尝试 3 个；OAuth 账号优先使用已映射的 `gpt-5.3-codex-spark`，未配置映射时也默认使用该 Spark 模型，避免故障排查走 `gpt-5.5` 导致响应过慢；ChatGPT internal `/backend-api/codex/responses` 请求不发送不兼容的 `max_output_tokens` 参数，AI 请求超时为 30 秒。
- 故障排查限流由后端 Redis 执行，当前限制为同一用户 5 分钟最多 10 次、当天最多 50 次；`analyze` 与 `notify-admin` 共用该限制。
- 新增认证用户接口 `POST /api/v1/troubleshooting/analyze`。
- 新增认证用户接口 `POST /api/v1/troubleshooting/notify-admin`；只有诊断结果需要管理员处理时前端才显示 `通知管理员` 按钮。点击后提示 `已通知管理员，请等待 5 分钟后重试。`；后端对该接口再次校验“只能排查请求失败”，并复用故障排查用户限流，避免直接调用通知接口刷管理员邮件。
- 管理员提醒复用邮件模板系统，新增事件 `troubleshooting.admin_notify`；收件人只读取现有 `account_quota_notify_emails` 配置里的已验证、未禁用邮箱，不查询或群发所有管理员邮箱；同一报错按 10 分钟桶跨用户去重，避免客户重复点击造成邮件轰炸；去重命中时返回 `管理员已收到，正在处理，请等待 5 分钟后重试。`。
- 新增全局右下角 `故障排查助手` 前端组件，仅登录后显示；未展开按钮和展开面板均可拖动并保存位置，拖动按钮后不会误触发打开；深色模式下悬浮按钮使用更高对比度背景，避免与全局背景重合。

**涉及文件：**

- `backend/internal/service/troubleshooting_assistant.go`
- `backend/internal/service/troubleshooting_assistant_test.go`
- `backend/internal/service/client_error_localization.go`
- `backend/internal/service/notification_email_service.go`
- `backend/internal/service/image_generation_intent.go`
- `backend/internal/service/openai_gateway_service.go`
- `backend/internal/handler/troubleshooting_handler.go`
- `backend/internal/handler/handler.go`
- `backend/internal/handler/wire.go`
- `backend/internal/server/routes/user.go`
- `backend/internal/service/wire.go`
- `backend/cmd/server/wire_gen.go`
- `frontend/src/api/troubleshooting.ts`
- `frontend/src/components/common/TroubleshootingAssistant.vue`
- `frontend/src/components/common/__tests__/TroubleshootingAssistant.spec.ts`
- `frontend/src/App.vue`

**验证：**

```bash
go test ./internal/service -run 'TroubleshootingAssistant|TroubleshootingEvidence|TroubleshootingAIModel|BuildTroubleshootingAIRequestBody|ExtractTroubleshootingAIText|RedisTroubleshootingRateLimiter|NotificationEmail|ImageGenerationPermission|ClientErrorMessage' -count=1
pnpm --dir frontend exec vitest run src/components/common/__tests__/TroubleshootingAssistant.spec.ts
go test ./cmd/server ./internal/service ./internal/handler ./internal/repository ./ent/... -count=1
pnpm --dir frontend run typecheck
本地 POST /api/v1/troubleshooting/analyze 验证证据不足时返回 source=ai、ai_available=true；证据命中时返回 source=rules 且不调用 AI
git diff --check
```

**同步官方后的复查：**

- 搜索 `TroubleshootingAssistant`、`troubleshooting/analyze`、`troubleshooting/notify-admin`、`troubleshooting.admin_notify`、`TroubleshootingAssistantService`、`SystemTroubleshootingEvidenceProvider`、`RedisTroubleshootingRateLimiter`、`OpenAITroubleshootingAIClient`。
- 如果官方新增类似客服/诊断助手，合并时必须确认仍由后端限制用途和访问量，不能让前端或普通用户直接访问上游账号。
- 回归 400/429/502/503/模型错误等典型报错，确认普通聊天被拒绝、限流不触发 AI；带 request id 的错误应优先使用系统日志，账号池已恢复时应提示用户重试，不应罗列多个可能原因；当结论是不需要管理员时，不应显示“是否需要联系管理员/暂时不需要联系管理员/建议提供给管理员的信息”段落；需要管理员处理时，确认 `通知管理员` 只发给已配置通知邮箱并在 10 分钟内去重；同时确认未展开的右下角按钮可拖动且深色模式下不与背景重合。

## 2026-07-05: 同账号同套餐订阅确认覆盖

**本地补丁：**

- 新增后端确认错误 `SUBSCRIPTION_DUPLICATE_CONFIRMATION_REQUIRED`，用于区分“重复订阅需要管理员确认”和普通分配失败。
- 管理端单用户分配订阅捕获该错误后弹出应用内确认框，文案明确说明继续后会覆盖已有订阅，不会创建第二条同账号同套餐记录。
- 确认后请求携带 `confirm_duplicate=true`，后端保留原订阅 ID，使用本次分配替换旧套餐周期，并重置开始时间、到期时间、日/周/月/总用量、窗口、分配人和备注。
- 取消确认时不再显示失败 toast；第一次重复检测请求跳过全局错误 toast，避免弹窗和错误提示同时出现。

**涉及文件：**

- `backend/internal/service/subscription_service.go`
- `backend/internal/service/subscription_assign_idempotency_test.go`
- `backend/internal/handler/admin/subscription_handler.go`
- `frontend/src/views/admin/SubscriptionsView.vue`
- `frontend/src/api/admin/subscriptions.ts`
- `frontend/src/types/index.ts`
- `frontend/src/i18n/locales/zh.ts`
- `frontend/src/i18n/locales/en.ts`

**验证：**

```bash
go test ./internal/service -run 'TestAssignSubscriptionDuplicateRequiresConfirmationWhenSemanticsMismatch|TestAssignSubscriptionConfirmedDuplicateReplacesExisting' -count=1
go test ./internal/service -run AssignSubscription -count=1
pnpm --dir frontend run typecheck
git diff --check
```

**同步官方后的复查：**

- 搜索 `SUBSCRIPTION_DUPLICATE_CONFIRMATION_REQUIRED`、`confirm_duplicate`、`duplicateConfirmTitle`、`assignSingleSubscriptionWithDuplicateConfirmation`。
- 如果官方后续调整订阅分配、兑换续期或幂等策略，需要继续保留三条边界：未确认时不修改已有订阅；确认后覆盖已有记录；不允许创建第二条未删除的同账号同套餐订阅。

## 2026-07-05: 错误响应与故障排查输出跟随界面语言

**本地补丁：**

- 前端请求已统一携带 `Accept-Language`，后端 `response.ErrorFrom` 在输出结构化错误时根据该 header 和 `reason` 替换展示用 `message`。
- 后端 `response.Error` / `response.ErrorWithDetails` 同步接入本地化，覆盖历史 handler 里大量直接返回的英文消息。
- 新增 `localizedErrorMessages` 首批覆盖认证、OAuth pending、订阅、故障排查、故障排查通知、运维错误日志、Turnstile、TOTP、兑换码等常见结构化错误。
- 新增 `localizedDirectErrorMessages` 和通用规则，覆盖 `Invalid ... ID`、`Invalid request: ...`、未登录、无权访问、服务未配置、功能未启用、请求体为空/读取失败、图片生成分组禁用、图片接口参数校验等常见直接响应。
- 新增兼容客户端错误消息本地化 helper，保持 OpenAI / Claude / Gemini 兼容响应结构不变，仅根据 `Accept-Language` 翻译本地生成的错误 `message`；上游原始错误不匹配本地规则时继续原样返回，避免影响客户排查。
- 未收录的 `reason` 继续返回原始 message，避免丢失动态错误详情；`reason` 与 `metadata` 字段保持不变，前端仍可按错误码做专门交互。
- 故障排查助手从 handler 传入 `Accept-Language`，本地规则、系统证据、AI 不可用提示和 AI prompt 均按语言输出；默认空语言仍保持中文。
- 管理员订阅进度接口不再把所有错误固定包装成 `Subscription not found`，真正订阅不存在仍返回 `SUBSCRIPTION_NOT_FOUND`，底层仓库/数据库错误原样透传。
- OpenAI gateway 的图片生成分组禁用响应不经过统一 `response.Error`，单独接入 `Accept-Language`，中文环境返回 `当前分组未启用图片生成`。
- 订阅分配弹窗捕获非重复确认类错误时改用统一错误提取工具，确保后端返回的 `message` 不会被泛化失败文案覆盖。

**涉及文件：**

- `backend/internal/pkg/response/response.go`
- `backend/internal/pkg/response/localization.go`
- `backend/internal/pkg/response/response_localization_test.go`
- `backend/internal/service/client_error_localization.go`
- `backend/internal/handler/admin/subscription_handler.go`
- `backend/internal/handler/gateway_handler.go`
- `backend/internal/handler/gemini_v1beta_handler.go`
- `backend/internal/handler/openai_gateway_handler.go`
- `backend/internal/handler/troubleshooting_handler.go`
- `backend/internal/service/troubleshooting_assistant.go`
- `backend/internal/service/troubleshooting_assistant_test.go`
- `backend/internal/service/subscription_service.go`
- `backend/internal/service/subscription_assign_idempotency_test.go`
- `backend/internal/service/image_generation_intent.go`
- `backend/internal/service/openai_gateway_service.go`
- `frontend/src/views/admin/SubscriptionsView.vue`
- `docs/fork-maintenance/2026-07.md`

**验证：**

```bash
go test -tags unit ./internal/pkg/response -run 'TestErrorFromLocalizes|TestErrorLocalizes' -count=1
go test ./internal/service -run 'TroubleshootingAssistant|TroubleshootingEvidence|TroubleshootingAIModel|BuildTroubleshootingAIRequestBody|ExtractTroubleshootingAIText|RedisTroubleshootingRateLimiter|ClientErrorMessage' -count=1
go test ./internal/handler/admin ./internal/handler -run Test -count=1
pnpm --dir frontend run typecheck
git diff --check
```

**同步官方后的复查：**

- 搜索 `localizedErrorMessages`、`localizedDirectErrorMessages`、`localizeErrorMessage`、`requestErrorLocale`、`Accept-Language`、`ErrorFrom`、`buildTroubleshootingAIPrompt` 和 `normalizeTroubleshootingLocale`。
- 新增后端业务错误时，优先使用 `infraerrors.*("STABLE_REASON", "...")`，再补中英文 message；避免只在 handler 里直接返回英文字符串。
- 如果官方新增统一错误国际化层，合并时需要保留本地行为：只替换展示 message，不改变 `reason` / `metadata`，未知 reason 原样兜底。

## 2026-07-06: 故障排查按钮收起到右侧把手

**本地补丁：**

- `TroubleshootingAssistant` 新增 `collapsed` 收起态，区别于未打开状态。
- 未展开的 `故障排查` 悬浮按钮右上角新增小关闭按钮，点击后完整按钮隐藏并在屏幕右侧显示更窄的箭头恢复按钮。
- 展开面板右上角关闭只恢复到原完整悬浮按钮，不进入右侧缩小状态；真正收起到侧边只通过悬浮按钮右上角小关闭按钮触发。
- 恢复按钮点击后直接按原保存位置展开面板；收起态只临时使用 `right: 0` 贴边，不覆盖用户拖拽保存的位置。
- 收起态写入 `localStorage` 的 `sub2api.troubleshootingAssistant.collapsed`；刷新后保持右侧小箭头，恢复/打开/关闭面板时写回未收起。
- 新增浅色/深色主题下的关闭按钮和恢复把手样式，避免深色模式下与页面背景重合。

**涉及文件：**

- `frontend/src/components/common/TroubleshootingAssistant.vue`
- `frontend/src/components/common/__tests__/TroubleshootingAssistant.spec.ts`
- `docs/fork-maintenance/2026-07.md`

**验证：**

```bash
pnpm --dir frontend exec vitest run src/components/common/__tests__/TroubleshootingAssistant.spec.ts
pnpm --dir frontend run typecheck
git diff --check
```

**同步官方后的复查：**

- 搜索 `collapsed`、`sub2api.troubleshootingAssistant.collapsed`、`assistant-fab-close`、`assistant-restore-handle`、`收起故障排查入口`、`关闭故障排查助手`、`恢复故障排查助手`。
- 如果官方重构全局悬浮入口或故障排查助手，需要保留“悬浮按钮右上角可收起、面板关闭恢复完整按钮、收起后只露出右侧小箭头且刷新后保持收起、恢复后使用原面板位置、深浅主题均可见”的交互。

## 2026-07-06: 部署脚本默认滚动三套线上后端

**本地补丁：**

- `deploy/local-gzip-binary-deploy.sh --apply --deploy` 默认滚动范围从 `ap1 + primary` 扩展为 `test + ap1 + primary`。
- 滚动顺序固定为低风险顺序：先 `/opt/sub2api-test-deploy` 的 `sub2api-test`，再 `/opt/sub2api-ap1-deploy` 的 `sub2api-ap1`，最后 `/opt/sub2api-deploy` 的 `sub2api`。
- test 环境不改 compose `image:`，而是备份并更新 `/opt/sub2api-test-deploy/.env` 的 `IMAGE_TAG`；primary/ap1 继续备份并更新 compose `image:`。
- 脚本新增 test 相关覆盖参数：`TEST_COMPOSE_DIR`、`TEST_SERVICE_NAME`、`TEST_CONTAINER`、`TEST_HEALTH_URL`、`TEST_PUBLIC_HEALTH_URL`，并补充 `AP1_PUBLIC_HEALTH_URL`。
- `docs/VPS_DEPLOY_NOTES.md` 与 `docs/CLOUDFLARE_PAGES_DEPLOY_CN.md` 同步改为默认三套线上后端发布；只部署 test 时仍需使用文档中的手动 test-only 流程，避免默认 `--deploy` 连带更新 ap1/primary。

**涉及文件：**

- `deploy/local-gzip-binary-deploy.sh`
- `docs/VPS_DEPLOY_NOTES.md`
- `docs/CLOUDFLARE_PAGES_DEPLOY_CN.md`
- `docs/fork-maintenance/2026-07.md`

**验证：**

```bash
bash -n deploy/local-gzip-binary-deploy.sh
IMAGE_TAG=sub2api:subapi-dryrun-test BASE_IMAGE=sub2api:base \
  deploy/local-gzip-binary-deploy.sh --deploy --skip-frontend-build --skip-backend-build
```

**同步官方后的复查：**

- 搜索 `TEST_COMPOSE_DIR`、`TEST_SERVICE_NAME`、`TEST_PUBLIC_HEALTH_URL`、`IMAGE_TAG=`、`sub2api-test`、`test -> ap1 -> primary`。
- 如果官方修改部署脚本，需保留“默认三套线上后端、test 写 `.env IMAGE_TAG`、primary/ap1 写 compose image、每套 healthy 后再继续”的行为。

## 2026-07-06: API 与前台错误提示补充多语言

**本地补丁：**

- 前端 API client 自生成错误改为走 i18n：网络异常、未知错误、登录状态过期不再硬编码英文。
- 账号测试弹窗的 fetch/SSE 兜底改为 i18n：HTTP 状态失败、响应体为空、未知错误、测试失败均走现有或新增翻译。
- 旧通用 OAuth composable 补充 i18n 兜底：生成授权链接失败、授权码兑换失败、缺少授权码或授权会话、Cookie 授权失败。
- 初始化向导连接/安装失败、代理测试失败等前端兜底改为使用对应 i18n key。
- 后端 `response.ErrorFrom` 高频 reason 补充中英文翻译：API Key、账号、分组、余额、邮箱、支付订单、头像、渠道等用户可见错误码。
- 继续排查后补齐 OpenAI OAuth 缺少授权码/session/state、缺少 Refresh Token 的本地提示，定时测试计划加载/创建/更新/删除/结果加载失败兜底，以及图片上传文件过大/非图片/读取失败的本地校验文案。
- 后端继续补充 OAuth pending、代理、运维、令牌、注册/邀请码、优惠码、退款、系统忙、用量清理、setup 参数、dashboard/ops 固定错误的多语言或中文映射；前端剩余硬编码 `throw new Error(...)` 为内部 control-flow/sentinel，不直接展示。

**涉及文件：**

- `frontend/src/api/client.ts`
- `frontend/src/api/__tests__/client.spec.ts`
- `frontend/src/components/account/AccountTestModal.vue`
- `frontend/src/components/admin/account/AccountTestModal.vue`
- `frontend/src/components/account/__tests__/AccountTestModal.spec.ts`
- `frontend/src/composables/useAccountOAuth.ts`
- `frontend/src/composables/__tests__/useAccountOAuth.spec.ts`
- `frontend/src/composables/useOpenAIOAuth.ts`
- `frontend/src/composables/__tests__/useOpenAIOAuth.spec.ts`
- `frontend/src/components/admin/account/ScheduledTestsPanel.vue`
- `frontend/src/components/admin/account/__tests__/ScheduledTestsPanel.spec.ts`
- `frontend/src/components/common/ImageUpload.vue`
- `frontend/src/components/common/__tests__/ImageUpload.spec.ts`
- `frontend/src/components/common/ProxySelector.vue`
- `frontend/src/views/setup/SetupWizardView.vue`
- `frontend/src/i18n/locales/zh.ts`
- `frontend/src/i18n/locales/en.ts`
- `backend/internal/pkg/response/localization.go`
- `backend/internal/pkg/response/response_localization_test.go`

**验证：**

```bash
pnpm --dir frontend exec vitest run src/api/__tests__/client.spec.ts
pnpm --dir frontend exec vitest run src/components/account/__tests__/AccountTestModal.spec.ts src/components/admin/account/__tests__/AccountTestModal.spec.ts
pnpm --dir frontend exec vitest run src/composables/__tests__/useAccountOAuth.spec.ts
pnpm --dir frontend exec vitest run src/composables/__tests__/useOpenAIOAuth.spec.ts src/components/common/__tests__/ImageUpload.spec.ts src/components/admin/account/__tests__/ScheduledTestsPanel.spec.ts
cd backend && go test ./internal/pkg/response -run 'TestErrorFromLocalizesStructuredErrorsByAcceptLanguage|TestErrorLocalizesCommonDirectHandlerMessagesByAcceptLanguage'
```

**同步官方后的复查：**

- 搜索 `Network error. Please check your connection.`、`HTTP error! status`、`No response body`、`Failed to generate auth URL`、`Connection failed`、`Test failed`、`Missing refresh token`、`File too large`、`Failed to load plans`、`API_KEY_EXPIRED`、`BALANCE_NOT_ENOUGH`、`GROUP_NOT_FOUND`、`REFRESH_TOKEN_EXPIRED`、`PROMO_CODE_EXPIRED`、`platform quota service not available`。
- 继续补充时优先走稳定 reason + i18n key，不要把上游原始错误或日志内部错误强行翻译成误导性客户文案。

## 2026-07-07: OpenAI quota 查询后刷新账号行运行态

**本地补丁：**

- `AccountUsageCell` 定义 `runtime-state-updated` 事件，用于把 cell 内部的 upstream quota 刷新结果同步给账号列表父级。
- 主动刷新 active usage 且先成功执行 `queryOpenAIQuota` 时，在 `/usage` 结果写入缓存后通知父级刷新外层账号行。
- 外部 OpenAI quota 查询回调完成后同样通知父级，避免账号行状态只依赖下一轮自动刷新。
- `AccountsView` 监听该事件后重置自动刷新缓存并执行增量刷新，保持账号表格行运行态、额度字段和用量单元格一致。

**涉及文件：**

- `frontend/src/components/account/AccountUsageCell.vue`
- `frontend/src/components/account/__tests__/AccountUsageCell.spec.ts`
- `frontend/src/views/admin/AccountsView.vue`
- `docs/fork-maintenance/2026-07.md`

**验证：**

```bash
pnpm --dir frontend exec vitest run src/components/account/__tests__/AccountUsageCell.spec.ts
pnpm --dir frontend run typecheck
git diff --check
```

**同步官方后的复查：**

- 搜索 `runtime-state-updated`、`handleAccountRuntimeStateUpdated`、`queryOpenAIQuota`、`loadActiveUsage`。
- 如果官方重构账号列表增量刷新、quota 查询或用量单元格缓存，需要确认手动查询 upstream quota 后会刷新外层账号行运行态，而不是只更新 `AccountUsageCell` 内部展示。

## 2026-07-05: a2 公网域名改为 a2t/test

**本地补丁：**

- Cloudflare Pages Worker 的正式域名映射调整为 `test.upit.top -> https://a2t.upit.top`，主环境 `ai.upit.top -> https://api.upit.top`、a1 `a1.upit.top -> https://ap1.upit.top` 保持不变。
- Pages Worker 回归测试、Pages public settings 注入测试同步改用 `test.upit.top` 前台和 `a2t.upit.top` API 回源。
- Cloudflare Pages 部署文档与 VPS 部署维护文档同步更新：第三套前台产物目录改为 `/tmp/sub2api-pages-test`，Pages 项目名改为 `sub2api-frontend-test`，public settings 注入来源改为 `https://a2t.upit.top/api/v1/settings/public`。
- Cloudflare Pages 实际项目同步创建为 `sub2api-frontend-test` 并发布 test 前台产物；旧 `sub2api-frontend-a2t` 仅作为历史项目保留，不再作为正式 test 前台项目。
- 第三套环境的 VPS 应用运行名同步从 `sub2api-ap2` 改为 `sub2api-test`：compose 目录为 `/opt/sub2api-test-deploy`，服务名和容器名为 `sub2api-test`。
- 2026-07-06 后续命名清理：Caddy 内部别名从 `ap2.51tokens.upit.top` 改为 `test.51tokens.upit.top`，Caddy 静态首页目录从 `/etc/caddy/ap2-home` 改为 `/etc/caddy/test-home`；PostgreSQL database 从 `sub2api_ap2` rename 为 `sub2api_test`；Redis 仍使用逻辑 DB 2，文档统一称为 test 环境 Redis DB2；旧空目录 `/opt/sub2api-ap2-deploy` 已归档为 `/opt/sub2api-ap2-deploy.archived-20260706083857`。
- test 环境 public settings 同步修正：`api_base_url=https://a2t.upit.top/51Token/v1`，`doc_url` 与低余额充值 URL 改为 `https://test.upit.top`。
- 故障排查助手补充明确错误短路：`No active subscription found for this group`、`SUBSCRIPTION_NOT_FOUND`、`subscription is invalid or expired` 等订阅不可用错误直接提示“订阅已过期，请联系管理员续期”；`no available accounts` / `account_select_failed` 等账号池为空错误直接提示“系统当前没有可用账户”，不再调用 AI 或输出多种可能原因。
- 继续补齐同类本地业务错误的明确结论：API Key 缺失/无效/停用/过期、API Key 额度用完、余额不足、订阅日/周/月/总量限额、RPM/并发限制、图片生成未启用、模型白名单不匹配、接口/客户端类型不允许、平台能力不支持等，均直接按系统结论返回，不再让 AI 重新猜测。
- 订阅分配重复确认修复：全局 `AppDialogHost` 固定使用高于普通表单弹窗的 `zIndex=100`，避免“已有相同订阅，确认覆盖”二次确认被分配表单遮挡；单用户分配在首次提交时保存 `group_id` / `validity_days` 快照，确认覆盖后用原始参数重试，避免表单重置后出现 `AssignSubscriptionRequest.GroupID required`。

**涉及文件：**

- `frontend/public/_worker.js`
- `frontend/src/cloudflare/__tests__/pages-worker.spec.ts`
- `frontend/src/cloudflare/__tests__/pages-config-injection.spec.ts`
- `frontend/src/views/home/components/__tests__/homeApiBase.spec.ts`
- `frontend/src/components/common/AppDialogHost.vue`
- `frontend/src/components/common/__tests__/AppDialogHost.spec.ts`
- `frontend/src/views/admin/SubscriptionsView.vue`
- `frontend/src/views/admin/__tests__/SubscriptionsView.duplicateAssign.spec.ts`
- `backend/internal/service/troubleshooting_assistant.go`
- `backend/internal/service/troubleshooting_assistant_test.go`
- `docs/CLOUDFLARE_PAGES_DEPLOY_CN.md`
- `docs/VPS_DEPLOY_NOTES.md`
- `docs/fork-maintenance/2026-07.md`

**验证：**

```bash
pnpm --dir frontend exec vitest run src/cloudflare/__tests__/pages-worker.spec.ts src/cloudflare/__tests__/pages-config-injection.spec.ts src/views/home/components/__tests__/homeApiBase.spec.ts
pnpm --dir frontend exec vitest run src/components/common/__tests__/AppDialogHost.spec.ts src/views/admin/__tests__/SubscriptionsView.duplicateAssign.spec.ts
pnpm --dir frontend run build
cd backend && go test ./internal/service -run 'TestTroubleshooting'
git diff --check
```

**同步官方后的复查：**

- 搜索 `PRODUCTION_API_ORIGINS_BY_HOST`、`test.upit.top`、`a2t.upit.top`、`sub2api-frontend-test`、`sub2api-pages-test`、`inject-pages-public-settings.mjs`。
- 不要把 `a2t.upit.top` 切到 Pages，它是 API 回源域名；不要停止 `sub2api-test`，Pages Worker 仍依赖它提供 `/api/*`、`/health`、`/51Token/*`。
