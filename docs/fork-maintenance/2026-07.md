# 2026-07 Fork 维护明细

本文件记录 2026-07 的非上游本地改动明细。主入口仍是 `docs/FORK_MAINTENANCE_CN.md`；这里用于承接较长的每日补丁说明、验证命令和同步官方后的复查点，避免主文档持续膨胀。

## 本地补丁记录表

| 日期 | 问题 | 本地修复 | 验证 | 后续同步复查 |
| --- | --- | --- | --- | --- |
| 2026-07-03 | 管理后台表格行操作里的“更多”菜单使用固定高度估算定位；账号管理菜单项增多后，靠近屏幕底部的行会把菜单渲染到视口下方，导致底部操作无法点击。用户管理等同类表格行菜单也存在同样风险。 | 新增公共定位工具 `frontend/src/utils/floatingMenuPosition.ts`，先按触发按钮和视口给出初始位置，再在菜单 Teleport 到 body 后读取真实 DOM 尺寸并夹紧到视口内；账号管理 `AccountActionMenu` 和用户管理行操作菜单都改用该逻辑，并设置 `max-height` + 内部滚动兜底，短屏或菜单项变化时不会撑出屏幕。 | `pnpm --dir frontend exec vitest run src/utils/__tests__/floatingMenuPosition.spec.ts src/components/admin/account/__tests__/AccountActionMenu.spark_shadow.spec.ts src/views/admin/__tests__/AccountsView.sparkShadow.spec.ts src/views/admin/__tests__/UsersView.spec.ts`；`pnpm --dir frontend run typecheck`；`pnpm --dir frontend run build`；`git diff --check`。 | 同步官方后搜索 `floatingMenuPosition`、`clampFloatingMenuPosition`、`getActionMenuPosition`、`AccountActionMenu`、`actionMenuContentRef`、`action-menu-content`。如果官方重构表格操作菜单或改用统一浮层库，需要继续确认账号管理、用户管理等表格底部行打开“更多”时不会超出屏幕；菜单项继续增减时不得恢复固定 `menuHeight=240` 作为唯一边界判断。 |
| 2026-07-05 | 管理员在订阅管理里给同一账号再次分配同一套餐时，后端只返回“订阅已存在/语义冲突”类错误，前端直接错误提示，管理员无法确认后继续覆盖旧订阅。 | 单用户分配订阅时，后端对需要人工确认的重复订阅返回 `SUBSCRIPTION_DUPLICATE_CONFIRMATION_REQUIRED`；前端捕获该错误并弹出确认框，说明继续后会用本次分配覆盖旧套餐周期，而不是创建第二条同账号同套餐记录。确认后带 `confirm_duplicate=true` 重试，原订阅记录保留 ID 但重置开始时间、到期时间、用量统计、分配人和备注。 | `go test ./internal/service -run 'TestAssignSubscriptionDuplicateRequiresConfirmationWhenSemanticsMismatch|TestAssignSubscriptionConfirmedDuplicateReplacesExisting' -count=1`；`go test ./internal/service -run AssignSubscription -count=1`；`pnpm --dir frontend run typecheck`；`git diff --check`。 | 同步官方后搜索 `SUBSCRIPTION_DUPLICATE_CONFIRMATION_REQUIRED`、`confirm_duplicate`、`duplicateConfirmTitle`、`assignSingleSubscriptionWithDuplicateConfirmation`。如果官方改造订阅分配/幂等逻辑，要保留“未确认不修改、确认后覆盖旧记录、不创建重复 user_id/group_id 订阅”的边界。 |
| 2026-07-06 | 继续排查后发现部分接口 `message` 和前台本地校验仍会显示英文：OpenAI OAuth 缺参数、定时测试计划加载/保存失败、图片上传本地校验，以及后端 OAuth/代理/运维、令牌、注册/邀请码、优惠码、退款、setup 和 dashboard/ops 固定错误。 | 继续扩展 `localizedErrorMessages` / `localizedDirectErrorMessages`，补充 auth/OAuth pending、proxy、ops、token、registration、promo/refund、usage cleanup、setup 参数、dashboard/ops settings 固定错误的中英文/中文映射；OpenAI OAuth、定时测试面板和 `ImageUpload` 改用 i18n key；前端剩余硬编码只保留内部 sentinel/control-flow error，不作为展示文案。 | `cd backend && go test ./internal/pkg/response -run 'TestErrorFromLocalizesStructuredErrorsByAcceptLanguage|TestErrorLocalizesCommonDirectHandlerMessagesByAcceptLanguage'`；`pnpm --dir frontend exec vitest run src/composables/__tests__/useOpenAIOAuth.spec.ts src/components/common/__tests__/ImageUpload.spec.ts src/components/admin/account/__tests__/ScheduledTestsPanel.spec.ts`。 | 同步官方后搜索 `missingExchangeParams`、`missingRefreshToken`、`loadPlansFailed`、`fileTooLarge`、`REFRESH_TOKEN_EXPIRED`、`PROMO_CODE_EXPIRED`、`Setup is not allowed`、`platform quota service not available`。低层幂等、内部事务、上游透传和字段级配置校验不要强行翻译成客户结论，除非对应接口确认为用户直接可见。 |
| 2026-07-07 | 账号管理里 OpenAI OAuth 账号手动查询 quota 后，`AccountUsageCell` 会刷新用量单元格，但外层账号表格行仍可能保留旧运行态/额度状态，造成列表状态与刚查询到的 upstream quota 不一致。 | `AccountUsageCell` 新增 `runtime-state-updated` 事件：主动查询 OpenAI quota 成功后，或外部 OpenAI quota query 回调完成后通知父级；`AccountsView` 收到事件后重置自动刷新缓存并执行增量刷新，让账号行状态、quota 运行态和用量单元格一起更新。 | `pnpm --dir frontend exec vitest run src/components/account/__tests__/AccountUsageCell.spec.ts`；`pnpm --dir frontend run typecheck`；`git diff --check`。 | 同步官方后搜索 `runtime-state-updated`、`handleAccountRuntimeStateUpdated`、`queryOpenAIQuota`、`loadActiveUsage`。如果官方改造账号列表刷新或用量单元格，需要继续保留“手动查询 upstream quota 后刷新外层账号行运行态”的行为，避免只更新 cell 内部缓存。 |
| 2026-07-07 | OpenAI 网关 fast/flex 策略需要支持用户与 OpenAI 上游账号双条件放行，避免全局 filter/block 规则影响少量可信用户和指定账号组合。 | `openai_fast_policy_settings.rules[]` 保留 `account_allowlist` 作为用户 ID 数组（字段名沿用旧命名以兼容已保存 JSON），并新增 `openai_account_allowlist` 作为 OpenAI 上游账号 ID 数组；规则匹配 scope + service_tier 后，配置了哪个放行列表就必须命中哪个列表，两个列表都配置时必须当前 API Key 所属用户 ID 与最终选中的 OpenAI 账号 ID 同时命中才直接 pass，否则继续执行原 action/model whitelist/fallback 逻辑；设置页改为分别搜索用户管理里的用户与 OpenAI 账号后选择加入放行列表，保存前去重、过滤空值。 | `cd backend && go test ./internal/service -run 'TestEvaluateOpenAIFastPolicy_(UserAllowlist|OpenAIAccount|AccountIDDoesNotBypass)|TestSetOpenAIFastPolicySettings_Validation|TestApplyOpenAIFastPolicyToBody|TestApplyOpenAIFastPolicyToWSResponseCreate'`；`pnpm --dir frontend exec vitest run src/utils/__tests__/openaiFastPolicyUsers.spec.ts src/utils/__tests__/openaiFastPolicyAccounts.spec.ts`；`pnpm --dir frontend run typecheck`；`git diff --check`。 | 同步官方后搜索 `account_allowlist`、`openai_account_allowlist`、`openAIFastPolicyAllowlisted`、`openaiFastPolicyForm.rules`。没有放行字段时旧策略行为不变；只配置用户列表则按用户放行，只配置 OpenAI 账号列表则按账号放行；两个都配置时必须同时命中。前端不能退回手填 ID。 |
| 2026-07-07 | 多个列表页的搜索框、状态/平台/分组等下拉筛选只保存在组件内存里，刷新页面或复制链接后会丢失当前筛选，用户需要重新输入搜索条件；账号管理等页面切到“全部状态/全部平台/全部分组”后，空值筛选又会被清掉，地址栏看不出当前是在查全部；部分页面首次进入时虽然已有默认筛选，但只有用户手动改筛选后才会写入地址栏。 | 新增 `useRouteQuerySync` 通用工具，页面声明需要同步的 query 字段后即可从地址栏回填并把筛选变化写回 URL；接入账号、用户、密钥、用户/管理用量、代理、公告、优惠码、兑换码、渠道、分组、订阅、用户/管理订单、可用渠道、渠道监控、风控日志、运维系统日志、推广记录等筛选入口。普通空搜索值从 URL 移除，无关 query 保留；各页面仍以原有默认筛选为准，原来默认 `active` 的账号/订阅状态会写成 `status=active`；用户手动选择“全部”时才写成 `all` token，例如 `status=all`、`platform=all`、`group_id=all`，回填时再映射回组件原本的空值或 `null/0`，不会把 `all` 传给后端；真实页面自动使用 Vue Router，同步路由状态；没有注入 Vue Router 的单测环境使用当前 URL/history 兜底；审查时补上“多个同步实例不互相覆盖 query”和“显式 all/default token”的回归测试，并让账号、用户、密钥、用量、订阅、风控、运维日志、推广记录等页面在 `restoreFromRoute()` 后立即 `syncToRoute()` 一次，确保首次进入也能看到默认/全部筛选参数；部分列表页的筛选变化统一重置到第一页再搜索。审查修复账号管理重复 `id` 列，以及用户管理已显示筛选变更后仍停留在旧页码的问题。 | `pnpm --dir frontend exec vitest run src/composables/__tests__/useRouteQuerySync.spec.ts src/views/user/__tests__/KeysView.spec.ts src/views/user/__tests__/UsageView.spec.ts src/views/admin/__tests__/UsageView.spec.ts src/views/admin/__tests__/UsersView.spec.ts`；`pnpm --dir frontend exec vitest run src/views/admin/__tests__/AccountsView.usageWindowsHint.spec.ts`；`pnpm --dir frontend run typecheck`；`pnpm --dir frontend run build`；`git diff --check`。 | 同步官方后搜索 `useRouteQuerySync`、`syncToRoute`、`restoreFromRoute`、`RouteQuerySyncField`、`defaultQueryValue`、`emptyQueryValue`。如果官方重构筛选栏或路由层，需要保留“筛选动作更新地址栏、刷新后先从 query 回填再请求、首次进入会同步默认/全部筛选参数、普通空搜索不污染 URL、原有非空默认筛选显式写原默认值、全部下拉条件显式写 `all`、多个筛选组件不覆盖彼此 query、组件单测不强依赖 router 注入、账号管理列 key 不重复、用户管理筛选变更回到第一页”的行为。 |
| 2026-07-07 | 线上已经不打算继续使用 Headroom，但 active 配置仍残留 primary 的 `headroom-main` 与 ap1 的 `headroom-a1`：`headroom-main` 已停止但还在 compose 中，`headroom-a1` 仍在运行；`sub2api` / `sub2api-ap1` 容器环境还保留 `GATEWAY_OPENAI_OAUTH_CODEX_RESPONSES_URL` 和 `HEADROOM_STATS_*`，容易在重建或开关恢复时把请求重新引回 Headroom。 | 线上备份 `/opt/sub2api-deploy` 与 `/opt/sub2api-ap1-deploy` 的 `docker-compose.yml` / `.env` 后，删除 `headroom-main`、`headroom-a1` 服务块，移除 `GATEWAY_OPENAI_OAUTH_CODEX_RESPONSES_*` / `HEADROOM_STATS_*` 环境变量；本地继续删除 Headroom responses override、后台 `openai_headroom_enabled` 开关、Headroom stats API/service、运维统计卡片和相关测试；同步更新 `docs/VPS_DEPLOY_NOTES.md`，明确当前 primary / ap1 / test 均不启用 Headroom。 | 线上验证：active compose / `.env` / `docker exec sub2api/sub2api-ap1/sub2api-test env` 均无 `HEADROOM` 或 `GATEWAY_OPENAI_OAUTH_CODEX_RESPONSES`；`docker ps -a` 无 `headroom-*` 容器；`https://api.upit.top/health`、`https://ap1.upit.top/health`、`https://a2t.upit.top/health` 均为 200；未带 key的 `https://api.upit.top/51Token/v1/models`、`https://ap1.upit.top/51Token/v1/models` 返回 401；本地搜索确认代码层不再有 Headroom sidecar/统计实现。 | 后续部署、回滚旧 compose 或同步官方时，继续搜索 `headroom-main`、`headroom-a1`、`GATEWAY_OPENAI_OAUTH_CODEX_RESPONSES`、`HEADROOM_STATS`、`openai_headroom_enabled`、`OpsHeadroomStatsCard`、`HeadroomStatsService`。当前生产和代码均不得恢复 Headroom sidecar、responses override、后台开关或 stats API。 |
| 2026-07-11 | 从上游 `main` 合并到 `subapi` 时，上游把后台设置、网关、用量统计、i18n 和分组表单拆分/重命名；如果按旧 fork 文件整体覆盖，会丢失本地非 fork 功能，或把上游新的视频计费、Codex/Grok/OpenAI 网关拆分结构改坏。 | 以最新版拆分结构为骨架重新落位本地改动：`SettingsView.vue` 保留用户与 OpenAI 账号双条件放行并修复 helper 同名冲突；`GroupSharedConfigSections.vue` 继续作为分组共享配置唯一渲染入口，把上游 Grok 视频计费字段、placeholder、最终价格预览迁入共享组件，避免 `GroupsView.vue` 再出现创建/编辑重复块；用量日志按上游 `usage_log_repo_insert/query/stats/trend/dashboard` 拆分补回 `presentation_multiplier` 插入、查询和展示倍率统计；OpenAI/Claude 响应处理按拆分后的 `gateway_*`、`openai_gateway_*` 文件补回展示用量重写、`image_output_tokens` 解析和 raw usage 计费隔离；OpenAI model router 的请求上下文注入落回 `openai_model_router.go`；public settings 注入补回 `ops_host_health_visible`；i18n 迁移到 `frontend/src/i18n/locales/*/admin/*.ts` split 文件，不恢复旧 `en.ts` / `zh.ts` 单体；`wire_gen.go` 通过 Wire 重新生成。 | `go generate ./cmd/server`；`go test ./cmd/server ./internal/service ./internal/handler ./internal/repository ./ent/... -count=1`；`go test -tags unit ./internal/service -run 'HandleStreamingResponse_RewritesClientUsageForPresentation|HandleStreamingResponse_RewritesSplitUsageAfterAccumulatedThreshold|ParseSSEUsage|ImageOutput|ForwardGrokMediaVideoGenerationPreservesImageToVideoModel|GetPublicSettings_ExposesHostHealthVisibilityFromEnvironmentConfig' -count=1`；`pnpm --dir frontend exec vitest run src/views/admin/__tests__/GroupsView.singleRenderControls.spec.ts src/views/admin/__tests__/groupsImagePricing.spec.ts src/views/admin/__tests__/SettingsView.spec.ts src/i18n/__tests__/localesNoKeyCollision.spec.ts`；`pnpm --dir frontend run typecheck`；`pnpm --dir frontend run build`；`git diff --check`。 | 后续同步官方时优先搜索 `GroupSharedConfigSections`、`video_rate_independent`、`video_price_480p`、`presentation_multiplier`、`usageLogSelectColumns`、`account_allowlist`、`openai_account_allowlist`、`rewriteClaudeUsageForPresentation`、`rewriteOpenAIUsageForPresentation`、`image_output_tokens`、`WithModelRouteRequestContext`、`ops_host_health_visible`、`useRouteQuerySync`、`user_search`、`filterUserSearchQuery`、`wire_gen.go`。若上游继续拆分/重命名文件或方法，不要恢复旧 monolith；应按最新结构重新实现本地需求，并确认共享配置只渲染一次、split i18n key 不缺失、usage 展示倍率仍只影响展示统计不改原始计费。 |
| 2026-07-11 | 普通管理员进入用量统计的“用户排行”时，总 Token 有值，但输入、输出、缓存 Token 三列为空；用户列也缺少管理员维护的用户备注；超级管理员原始视图正常。 | 修复上游拆分后 `GetUserBreakdownStatsForView` 只聚合总 Token、遗漏三个分项的问题：展示视图按同一 `presentation_multiplier` 分别聚合输入、输出（保留图片输出 token fallback）和缓存创建/读取 Token，并补齐 Scan；原始和展示查询都从 `users.notes` 返回管理员备注，用户排行在邮箱与用户 ID 下方显示非空备注。原始统计、计费和超级管理员视图不变。权限边界保持为普通用户无法访问后台排行，普通管理员查看倍率展示统计，超级管理员查看原始统计。 | `cd backend && go test ./internal/repository -run TestUsageLogRepositoryGetUserBreakdownStatsForViewUsesPresentationMultiplier -count=1`；`cd backend && go test -tags unit ./internal/repository -run TestGetUserBreakdownStatsRequestTypeIncludesLegacyFallback -count=1`；`cd backend && go test ./internal/handler/admin -run 'TestGetUserBreakdown_UsesPresentationViewForOrdinaryAdmin|TestGetUserBreakdown_UsesRawViewForSuperAdmin' -count=1`；`cd backend && go test -tags unit ./internal/server/middleware -run 'TestAdminAuthJWTValidatesTokenVersion/ordinary_user_cannot_access_user_breakdown' -count=1`；`pnpm --dir frontend exec vitest run src/components/admin/usage/__tests__/UserTokenRanking.spec.ts`；`pnpm --dir frontend run typecheck`；`pnpm --dir frontend run build`；`git diff --check`。 | 同步官方后检查原始和展示版 `GetUserBreakdownStats` 的 SELECT、`GROUP BY` 与 Scan 必须同时包含 `users.notes`，展示版还必须包含 `input_tokens`、`output_tokens`、`cache_tokens`、`total_tokens`；用户排行按任一分项排序时也必须使用实际返回的展示统计，不能只返回总 Token；同时保留普通用户 403、普通管理员展示视图和超级管理员原始视图的角色测试。 |
| 2026-07-11 | 客户端遇到订阅过期、套餐日/周/月/总额度耗尽、余额不足或上游账号不可用时，不同网关协议仍可能只返回泛化 500/502 和英文消息；未携带 `Accept-Language` 的 CLI 更难判断是否需要续期。A1 在 `2026-07-10 15:50` 附近还出现 Cloudflare 524，源站统计显示 OpenAI 账号 53 的首 Token 等待多次超过 120 秒，最长超过 1500 秒，而 VPS CPU 正常。 | 扩展服务层客户端错误目录并接入 API Key 鉴权、Anthropic、OpenAI Responses、Chat Completions、Gemini 及流内终止事件：中文语言头返回中文，英文语言头返回英文，缺省语言头对已知本地错误返回英文加中文说明；未知上游原文保持不变。订阅验证不再把 `ApplicationError.Error()` 调试串返回客户端，改为保留具体 reason 和干净 message；套餐额度状态刷新失败改为 503。OpenAI 响应头超时识别为 504 `upstream_timeout`，不再伪装成普通 502；三套 Cloudflare 回源统一设置 `GATEWAY_OPENAI_RESPONSE_HEADER_TIMEOUT=100`，在 Cloudflare 120 秒前返回可控错误，流式 keepalive 与已建立流的总时长不变。 | `cd backend && go test ./internal/service -run TestClientErrorMessageForAcceptLanguageLocalizesBillingAndTimeoutErrors -count=1`；`cd backend && go test ./internal/handler -run TestGatewayProtocolErrorsLocalizePackageQuota -count=1`；`cd backend && go test ./internal/server/middleware -run TestGatewayMiddlewareErrorWritersLocalizeMessages -count=1`；`cd backend && go test -tags unit ./internal/service -run 'TestHandleOpenAIUpstreamTransportError' -count=1`；全量包级测试、后端编译检查、前端 typecheck/build 与三套健康检查。 | 同步官方后搜索 `ClientErrorMessageForAcceptLanguage`、`localizedClientErrorMessages`、`canonicalEnglishClientErrorMessages`、`isOpenAIResponseHeaderTimeout`、`openAITransportTimeoutFailoverBody`、`GATEWAY_OPENAI_RESPONSE_HEADER_TIMEOUT`。必须保持协议 envelope、状态码、未知上游原文和流式 keepalive 不变；Cloudflare 回源超时必须低于 120 秒。 |
| 2026-07-11 | A1 的拼车100、拼车200日额度耗尽后会按各自配置切换到接力套餐，但接力分组强制模型为 `gpt-5.3-codex-spark`，客户端原请求携带的 `reasoning.summary` 被原样转发，上游返回 400，用户侧表现为自动接力不可用。 | OpenAI Responses 最终上游模型为 Spark 时，仅删除该模型明确不支持的 `reasoning.summary`，保留 `reasoning.effort` 和其他字段；非 Spark 模型保持原请求不变。A1 保持 `拼车100(10) -> 接力(13)` 和 `拼车200(14) -> 接力(13)` 两条显式配置。自动接力只在请求上下文中切换计费订阅和分组，不永久修改 API key 绑定分组。 | `cd backend && go test ./internal/service -run 'TestOpenAIGatewayService_Forward_(StripsReasoningSummaryForSparkAPIKey|PreservesReasoningSummaryForNonSparkAPIKey)' -count=1`；`cd backend && go test ./internal/handler -run '^TestEnforceBillingEligibilityWithFallback_' -count=1`；分别使用已超额的拼车100和拼车200账号请求 A1，日志均确认进入组 13、模型为 Spark 且 HTTP 200；数据库确认组 10、14 的 `quota_fallback_group_id` 都为 13。 | 同步官方后搜索 `stripCodexSparkReasoningSummary`、`reasoning.summary`、`enforceBillingEligibilityWithFallback`、`ResolveQuotaFallback`。必须保持“只处理最终模型为 Spark 的请求、其他模型不删除 summary、只有显式配置接力目标的源套餐才接力、接力不永久改写 API key 分组”的边界。 |
| 2026-07-15 | 账号管理顶部“更多操作”按钮在移动端会因操作按钮换行而位于小型 flex 子容器中；下拉菜单使用该子容器的 `right: 0` 定位，菜单宽度较大时左边会超出视口，导致菜单项无法点击。 | `frontend/src/views/admin/AccountsView.vue` 的顶部工具菜单改为固定视口定位：打开时根据触发按钮的 `getBoundingClientRect()` 计算位置，挂载后按实际菜单尺寸通过 `clampFloatingMenuPosition` 夹紧左右和上下边界；窗口缩放、页面滚动时重新校正，菜单宽度继续限制为视口宽度减去边距。补充 320px 窄屏下菜单左边界和宽菜单夹紧测试。 | `pnpm --dir frontend exec vitest run src/utils/__tests__/floatingMenuPosition.spec.ts`；`pnpm --dir frontend run typecheck`；`pnpm --dir frontend run build`；`git diff --check`。 | 同步官方后搜索 `toggleAccountToolsDropdown`、`accountToolsMenuPosition`、`accountToolsMenuStyle`、`accountToolsMenuRef`、`clampFloatingMenuPosition` 和 `admin.accounts.moreActions`。如果官方改用 Dropdown / Popover / Floating UI，必须保留移动端菜单不超出左右视口、菜单底部可见或可滚动、按钮换行后仍以触发按钮为锚点的行为。 |

## 详细记录

## 2026-07-03: 表格行操作菜单底部防溢出

**本地补丁：**

- 新增 `floatingMenuPosition` 公共定位工具，将浮层位置按视口边界夹紧，并返回可用最大高度。
- 账号管理行操作 `AccountActionMenu` 在 Teleport 到 body 后读取真实菜单尺寸，重新校正 top/left，并设置 `max-height` 与 `overflow-y: auto`。
- 用户管理行操作菜单复用同一套定位逻辑，避免同类“更多”菜单在底部行打开后超出屏幕。
- 初始定位仍保留桌面端靠近点击点、移动端围绕触发按钮的原交互；本次不改业务菜单项和按钮行为。

**涉及文件：**

- `frontend/src/utils/floatingMenuPosition.ts`
- `frontend/src/utils/__tests__/floatingMenuPosition.spec.ts`
- `frontend/src/components/admin/account/AccountActionMenu.vue`
- `frontend/src/views/admin/AccountsView.vue`
- `frontend/src/views/admin/UsersView.vue`
- `docs/fork-maintenance/2026-07.md`

**验证：**

```bash
pnpm --dir frontend exec vitest run src/utils/__tests__/floatingMenuPosition.spec.ts src/components/admin/account/__tests__/AccountActionMenu.spark_shadow.spec.ts src/views/admin/__tests__/AccountsView.sparkShadow.spec.ts src/views/admin/__tests__/UsersView.spec.ts
pnpm --dir frontend run typecheck
pnpm --dir frontend run build
git diff --check
```

**同步官方后的复查：**

- 搜索 `floatingMenuPosition`、`clampFloatingMenuPosition`、`getActionMenuPosition`、`AccountActionMenu`、`actionMenuContentRef` 和 `action-menu-content`。
- 如果官方后续引入统一 Dropdown / Popover / Floating UI，优先收敛到官方浮层实现，但必须保留“按真实菜单高度校正位置”和“短屏内可滚动”的行为。
- 回归账号管理、用户管理以及后续新增的表格行“更多”菜单：在最后几行打开菜单时，底部操作项应保持在视口内可点击。

## 2026-07-05: 同账号同套餐订阅确认覆盖

**本地补丁：**

- 新增后端确认错误 `SUBSCRIPTION_DUPLICATE_CONFIRMATION_REQUIRED`，用于区分“重复订阅需要管理员确认”和普通分配失败。
- 管理端单用户分配订阅捕获该错误后弹出应用内确认框，文案明确说明继续后会覆盖已有订阅，不会创建第二条同账号同套餐记录。
- 确认后请求携带 `confirm_duplicate=true`，后端保留原订阅 ID，使用本次分配替换旧套餐周期，并重置开始时间、到期时间、日/周/月/总用量、窗口、分配人和备注。
- 取消确认时不再显示失败 toast；第一次重复检测请求跳过全局错误 toast，避免弹窗和错误提示同时出现。

**涉及文件：**

- `backend/internal/service/subscription_service.go`
- `backend/internal/service/subscription_assign_idempotency_test.go`
- `backend/internal/handler/admin/subscription_handler.go`
- `frontend/src/views/admin/SubscriptionsView.vue`
- `frontend/src/api/admin/subscriptions.ts`
- `frontend/src/types/index.ts`
- `frontend/src/i18n/locales/zh/admin/channels.ts`
- `frontend/src/i18n/locales/en/admin/channels.ts`

**验证：**

```bash
go test ./internal/service -run 'TestAssignSubscriptionDuplicateRequiresConfirmationWhenSemanticsMismatch|TestAssignSubscriptionConfirmedDuplicateReplacesExisting' -count=1
go test ./internal/service -run AssignSubscription -count=1
pnpm --dir frontend run typecheck
git diff --check
```

**同步官方后的复查：**

- 搜索 `SUBSCRIPTION_DUPLICATE_CONFIRMATION_REQUIRED`、`confirm_duplicate`、`duplicateConfirmTitle`、`assignSingleSubscriptionWithDuplicateConfirmation`。
- 如果官方后续调整订阅分配、兑换续期或幂等策略，需要继续保留三条边界：未确认时不修改已有订阅；确认后覆盖已有记录；不允许创建第二条未删除的同账号同套餐订阅。

## 2026-07-05: 错误响应跟随界面语言

**本地补丁：**

- 前端请求已统一携带 `Accept-Language`，后端 `response.ErrorFrom` 在输出结构化错误时根据该 header 和 `reason` 替换展示用 `message`。
- 后端 `response.Error` / `response.ErrorWithDetails` 同步接入本地化，覆盖历史 handler 里大量直接返回的英文消息。
- 新增 `localizedErrorMessages` 首批覆盖认证、OAuth pending、订阅、运维错误日志、Turnstile、TOTP、兑换码等常见结构化错误。
- 新增 `localizedDirectErrorMessages` 和通用规则，覆盖 `Invalid ... ID`、`Invalid request: ...`、未登录、无权访问、服务未配置、功能未启用、请求体为空/读取失败、图片生成分组禁用、图片接口参数校验等常见直接响应。
- 新增兼容客户端错误消息本地化 helper，保持 OpenAI / Claude / Gemini 兼容响应结构不变，仅根据 `Accept-Language` 翻译本地生成的错误 `message`；上游原始错误不匹配本地规则时继续原样返回，避免影响客户排查。
- 未收录的 `reason` 继续返回原始 message，避免丢失动态错误详情；`reason` 与 `metadata` 字段保持不变，前端仍可按错误码做专门交互。
- 管理员订阅进度接口不再把所有错误固定包装成 `Subscription not found`，真正订阅不存在仍返回 `SUBSCRIPTION_NOT_FOUND`，底层仓库/数据库错误原样透传。
- OpenAI gateway 的图片生成分组禁用响应不经过统一 `response.Error`，单独接入 `Accept-Language`，中文环境返回 `当前分组未启用图片生成`。
- 订阅分配弹窗捕获非重复确认类错误时改用统一错误提取工具，确保后端返回的 `message` 不会被泛化失败文案覆盖。

**涉及文件：**

- `backend/internal/pkg/response/response.go`
- `backend/internal/pkg/response/localization.go`
- `backend/internal/pkg/response/response_localization_test.go`
- `backend/internal/service/client_error_localization.go`
- `backend/internal/handler/admin/subscription_handler.go`
- `backend/internal/handler/gateway_handler.go`
- `backend/internal/handler/gemini_v1beta_handler.go`
- `backend/internal/handler/openai_gateway_handler.go`
- `backend/internal/service/subscription_service.go`
- `backend/internal/service/subscription_assign_idempotency_test.go`
- `backend/internal/service/image_generation_intent.go`
- `backend/internal/service/openai_gateway_service.go`
- `frontend/src/views/admin/SubscriptionsView.vue`
- `docs/fork-maintenance/2026-07.md`

**验证：**

```bash
go test -tags unit ./internal/pkg/response -run 'TestErrorFromLocalizes|TestErrorLocalizes' -count=1
go test ./internal/service -run 'ClientErrorMessage' -count=1
go test ./internal/handler/admin ./internal/handler -run Test -count=1
pnpm --dir frontend run typecheck
git diff --check
```

**同步官方后的复查：**

- 搜索 `localizedErrorMessages`、`localizedDirectErrorMessages`、`localizeErrorMessage`、`requestErrorLocale`、`Accept-Language`、`ErrorFrom`。
- 新增后端业务错误时，优先使用 `infraerrors.*("STABLE_REASON", "...")`，再补中英文 message；避免只在 handler 里直接返回英文字符串。
- 如果官方新增统一错误国际化层，合并时需要保留本地行为：只替换展示 message，不改变 `reason` / `metadata`，未知 reason 原样兜底。

## 2026-07-05: a2 公网域名改为 a2t/test

**本地补丁：**

- Cloudflare Pages Worker 的正式域名映射调整为 `test.upit.top -> https://a2t.upit.top`，主环境 `ai.upit.top -> https://api.upit.top`、a1 `a1.upit.top -> https://ap1.upit.top` 保持不变。
- Pages Worker 回归测试、Pages public settings 注入测试同步改用 `test.upit.top` 前台和 `a2t.upit.top` API 回源。
- Cloudflare Pages 部署文档与 VPS 部署维护文档同步更新：第三套前台产物目录改为 `/tmp/sub2api-pages-test`，Pages 项目名改为 `sub2api-frontend-test`，public settings 注入来源改为 `https://a2t.upit.top/api/v1/settings/public`。
- Cloudflare Pages 实际项目同步创建为 `sub2api-frontend-test` 并发布 test 前台产物；旧 `sub2api-frontend-a2t` 仅作为历史项目保留，不再作为正式 test 前台项目。
- 第三套环境的 VPS 应用运行名同步从 `sub2api-ap2` 改为 `sub2api-test`：compose 目录为 `/opt/sub2api-test-deploy`，服务名和容器名为 `sub2api-test`。
- 2026-07-06 后续命名清理：Caddy 内部别名从 `ap2.51tokens.upit.top` 改为 `test.51tokens.upit.top`，Caddy 静态首页目录从 `/etc/caddy/ap2-home` 改为 `/etc/caddy/test-home`；PostgreSQL database 从 `sub2api_ap2` rename 为 `sub2api_test`；Redis 仍使用逻辑 DB 2，文档统一称为 test 环境 Redis DB2；旧空目录 `/opt/sub2api-ap2-deploy` 已归档为 `/opt/sub2api-ap2-deploy.archived-20260706083857`。
- test 环境 public settings 同步修正：`api_base_url=https://a2t.upit.top/51Token/v1`，`doc_url` 与低余额充值 URL 改为 `https://test.upit.top`。
- 订阅分配重复确认修复：全局 `AppDialogHost` 固定使用高于普通表单弹窗的 `zIndex=100`，避免“已有相同订阅，确认覆盖”二次确认被分配表单遮挡；单用户分配在首次提交时保存 `group_id` / `validity_days` 快照，确认覆盖后用原始参数重试，避免表单重置后出现 `AssignSubscriptionRequest.GroupID required`。

**涉及文件：**

- `frontend/public/_worker.js`
- `frontend/src/cloudflare/__tests__/pages-worker.spec.ts`
- `frontend/src/cloudflare/__tests__/pages-config-injection.spec.ts`
- `frontend/src/views/home/components/__tests__/homeApiBase.spec.ts`
- `frontend/src/components/common/AppDialogHost.vue`
- `frontend/src/components/common/__tests__/AppDialogHost.spec.ts`
- `frontend/src/views/admin/SubscriptionsView.vue`
- `frontend/src/views/admin/__tests__/SubscriptionsView.duplicateAssign.spec.ts`
- `docs/CLOUDFLARE_PAGES_DEPLOY_CN.md`
- `docs/VPS_DEPLOY_NOTES.md`
- `docs/fork-maintenance/2026-07.md`

**验证：**

```bash
pnpm --dir frontend exec vitest run src/cloudflare/__tests__/pages-worker.spec.ts src/cloudflare/__tests__/pages-config-injection.spec.ts src/views/home/components/__tests__/homeApiBase.spec.ts
pnpm --dir frontend exec vitest run src/components/common/__tests__/AppDialogHost.spec.ts src/views/admin/__tests__/SubscriptionsView.duplicateAssign.spec.ts
pnpm --dir frontend run build
git diff --check
```

**同步官方后的复查：**

- 搜索 `PRODUCTION_API_ORIGINS_BY_HOST`、`test.upit.top`、`a2t.upit.top`、`sub2api-frontend-test`、`sub2api-pages-test`、`inject-pages-public-settings.mjs`。
- 不要把 `a2t.upit.top` 切到 Pages，它是 API 回源域名；不要停止 `sub2api-test`，Pages Worker 仍依赖它提供 `/api/*`、`/health`、`/51Token/*`。

## 2026-07-06: 部署脚本默认滚动三套线上后端

**本地补丁：**

- `deploy/local-gzip-binary-deploy.sh --apply --deploy` 默认滚动范围从 `ap1 + primary` 扩展为 `test + ap1 + primary`。
- 滚动顺序固定为低风险顺序：先 `/opt/sub2api-test-deploy` 的 `sub2api-test`，再 `/opt/sub2api-ap1-deploy` 的 `sub2api-ap1`，最后 `/opt/sub2api-deploy` 的 `sub2api`。
- test 环境不改 compose `image:`，而是备份并更新 `/opt/sub2api-test-deploy/.env` 的 `IMAGE_TAG`；primary/ap1 继续备份并更新 compose `image:`。
- 脚本新增 test 相关覆盖参数：`TEST_COMPOSE_DIR`、`TEST_SERVICE_NAME`、`TEST_CONTAINER`、`TEST_HEALTH_URL`、`TEST_PUBLIC_HEALTH_URL`，并补充 `AP1_PUBLIC_HEALTH_URL`。
- `docs/VPS_DEPLOY_NOTES.md` 与 `docs/CLOUDFLARE_PAGES_DEPLOY_CN.md` 同步改为默认三套线上后端发布；只部署 test 时仍需使用文档中的手动 test-only 流程，避免默认 `--deploy` 连带更新 ap1/primary。

**涉及文件：**

- `deploy/local-gzip-binary-deploy.sh`
- `docs/VPS_DEPLOY_NOTES.md`
- `docs/CLOUDFLARE_PAGES_DEPLOY_CN.md`
- `docs/fork-maintenance/2026-07.md`

**验证：**

```bash
bash -n deploy/local-gzip-binary-deploy.sh
IMAGE_TAG=sub2api:subapi-dryrun-test BASE_IMAGE=sub2api:base \
  deploy/local-gzip-binary-deploy.sh --deploy --skip-frontend-build --skip-backend-build
```

**同步官方后的复查：**

- 搜索 `TEST_COMPOSE_DIR`、`TEST_SERVICE_NAME`、`TEST_PUBLIC_HEALTH_URL`、`IMAGE_TAG=`、`sub2api-test`、`test -> ap1 -> primary`。
- 如果官方修改部署脚本，需保留“默认三套线上后端、test 写 `.env IMAGE_TAG`、primary/ap1 写 compose image、每套 healthy 后再继续”的行为。

## 2026-07-06: API 与前台错误提示补充多语言

**本地补丁：**

- 前端 API client 自生成错误改为走 i18n：网络异常、未知错误、登录状态过期不再硬编码英文。
- 账号测试弹窗的 fetch/SSE 兜底改为 i18n：HTTP 状态失败、响应体为空、未知错误、测试失败均走现有或新增翻译。
- 旧通用 OAuth composable 补充 i18n 兜底：生成授权链接失败、授权码兑换失败、缺少授权码或授权会话、Cookie 授权失败。
- 初始化向导连接/安装失败、代理测试失败等前端兜底改为使用对应 i18n key。
- 后端 `response.ErrorFrom` 高频 reason 补充中英文翻译：API Key、账号、分组、余额、邮箱、支付订单、头像、渠道等用户可见错误码。
- 继续排查后补齐 OpenAI OAuth 缺少授权码/session/state、缺少 Refresh Token 的本地提示，定时测试计划加载/创建/更新/删除/结果加载失败兜底，以及图片上传文件过大/非图片/读取失败的本地校验文案。
- 后端继续补充 OAuth pending、代理、运维、令牌、注册/邀请码、优惠码、退款、系统忙、用量清理、setup 参数、dashboard/ops 固定错误的多语言或中文映射；前端剩余硬编码 `throw new Error(...)` 为内部 control-flow/sentinel，不直接展示。

**涉及文件：**

- `frontend/src/api/client.ts`
- `frontend/src/api/__tests__/client.spec.ts`
- `frontend/src/components/account/AccountTestModal.vue`
- `frontend/src/components/admin/account/AccountTestModal.vue`
- `frontend/src/components/account/__tests__/AccountTestModal.spec.ts`
- `frontend/src/composables/useAccountOAuth.ts`
- `frontend/src/composables/__tests__/useAccountOAuth.spec.ts`
- `frontend/src/composables/useOpenAIOAuth.ts`
- `frontend/src/composables/__tests__/useOpenAIOAuth.spec.ts`
- `frontend/src/components/admin/account/ScheduledTestsPanel.vue`
- `frontend/src/components/admin/account/__tests__/ScheduledTestsPanel.spec.ts`
- `frontend/src/components/common/ImageUpload.vue`
- `frontend/src/components/common/__tests__/ImageUpload.spec.ts`
- `frontend/src/components/common/ProxySelector.vue`
- `frontend/src/views/setup/SetupWizardView.vue`
- `frontend/src/i18n/locales/zh/admin/accounts.ts`
- `frontend/src/i18n/locales/en/admin/accounts.ts`
- `frontend/src/i18n/locales/zh/common.ts`
- `frontend/src/i18n/locales/en/common.ts`
- `backend/internal/pkg/response/localization.go`
- `backend/internal/pkg/response/response_localization_test.go`

**验证：**

```bash
pnpm --dir frontend exec vitest run src/api/__tests__/client.spec.ts
pnpm --dir frontend exec vitest run src/components/account/__tests__/AccountTestModal.spec.ts src/components/admin/account/__tests__/AccountTestModal.spec.ts
pnpm --dir frontend exec vitest run src/composables/__tests__/useAccountOAuth.spec.ts
pnpm --dir frontend exec vitest run src/composables/__tests__/useOpenAIOAuth.spec.ts src/components/common/__tests__/ImageUpload.spec.ts src/components/admin/account/__tests__/ScheduledTestsPanel.spec.ts
cd backend && go test ./internal/pkg/response -run 'TestErrorFromLocalizesStructuredErrorsByAcceptLanguage|TestErrorLocalizesCommonDirectHandlerMessagesByAcceptLanguage'
```

**同步官方后的复查：**

- 搜索 `Network error. Please check your connection.`、`HTTP error! status`、`No response body`、`Failed to generate auth URL`、`Connection failed`、`Test failed`、`Missing refresh token`、`File too large`、`Failed to load plans`、`API_KEY_EXPIRED`、`BALANCE_NOT_ENOUGH`、`GROUP_NOT_FOUND`、`REFRESH_TOKEN_EXPIRED`、`PROMO_CODE_EXPIRED`、`platform quota service not available`。
- 继续补充时优先走稳定 reason + i18n key，不要把上游原始错误或日志内部错误强行翻译成误导性客户文案。

## 2026-07-07: OpenAI quota 查询后刷新账号行运行态

**本地补丁：**

- `AccountUsageCell` 定义 `runtime-state-updated` 事件，用于把 cell 内部的 upstream quota 刷新结果同步给账号列表父级。
- 主动刷新 active usage 且先成功执行 `queryOpenAIQuota` 时，在 `/usage` 结果写入缓存后通知父级刷新外层账号行。
- 外部 OpenAI quota 查询回调完成后同样通知父级，避免账号行状态只依赖下一轮自动刷新。
- `AccountsView` 监听该事件后重置自动刷新缓存并执行增量刷新，保持账号表格行运行态、额度字段和用量单元格一致。

**涉及文件：**

- `frontend/src/components/account/AccountUsageCell.vue`
- `frontend/src/components/account/__tests__/AccountUsageCell.spec.ts`
- `frontend/src/views/admin/AccountsView.vue`
- `docs/fork-maintenance/2026-07.md`

**验证：**

```bash
pnpm --dir frontend exec vitest run src/components/account/__tests__/AccountUsageCell.spec.ts
pnpm --dir frontend run typecheck
git diff --check
```

**同步官方后的复查：**

- 搜索 `runtime-state-updated`、`handleAccountRuntimeStateUpdated`、`queryOpenAIQuota`、`loadActiveUsage`。
- 如果官方重构账号列表增量刷新、quota 查询或用量单元格缓存，需要确认手动查询 upstream quota 后会刷新外层账号行运行态，而不是只更新 `AccountUsageCell` 内部展示。

## 2026-07-07: OpenAI fast mode 用户与账号双条件放行

**本地补丁：**

- `OpenAIFastPolicyRule` 新增 `account_allowlist` 字段，保存为用户管理里的用户 ID 数组；字段名保留 `account_allowlist` 只为兼容已保存 JSON。
- `OpenAIFastPolicyRule` 新增 `openai_account_allowlist` 字段，保存 OpenAI 上游账号 ID 数组。
- API Key 鉴权后把当前请求用户 ID 写入 request context；网关评估 fast/flex 策略时，先匹配规则的 scope 与 service_tier；若当前规则配置了用户放行列表，则用户必须命中；若配置了 OpenAI 账号放行列表，则最终选中的上游账号必须命中；两个列表都配置时必须同时命中才直接返回 pass。
- 放行列表只影响当前匹配规则，不会让 flex 规则里的放行组合跳过后续 priority 规则；只命中用户或只命中账号时，在双列表规则下都不会误放行。
- 后端保存设置时校验用户 ID 和 OpenAI 账号 ID 都必须为正数；前端设置页分别搜索用户管理列表与 OpenAI 账号列表并选择加入放行列表，保存前去重并过滤空值/非法值，旧配置里的 ID 查不到详情时仍以 `#ID` 展示。
- 旧配置兼容：没有 `account_allowlist` / `openai_account_allowlist` 字段时，原有 action、model whitelist、fallback 行为不变；只配置其中一个列表时仍按单列表放行。

**涉及文件：**

- `backend/internal/service/settings_view.go`
- `backend/internal/service/setting_service.go`
- `backend/internal/service/openai_gateway_service.go`
- `backend/internal/service/openai_fast_policy_test.go`
- `backend/internal/server/middleware/api_key_auth.go`
- `backend/internal/handler/dto/settings.go`
- `frontend/src/api/admin/settings.ts`
- `frontend/src/views/admin/SettingsView.vue`
- `frontend/src/utils/openaiFastPolicyUsers.ts`
- `frontend/src/utils/__tests__/openaiFastPolicyUsers.spec.ts`
- `frontend/src/utils/openaiFastPolicyAccounts.ts`
- `frontend/src/utils/__tests__/openaiFastPolicyAccounts.spec.ts`
- `frontend/src/i18n/locales/en/admin/settings.ts`
- `frontend/src/i18n/locales/zh/admin/settings.ts`
- `docs/fork-maintenance/2026-07.md`

**验证：**

```bash
cd backend && go test ./internal/service -run 'TestEvaluateOpenAIFastPolicy_(UserAllowlist|OpenAIAccount|AccountIDDoesNotBypass)|TestSetOpenAIFastPolicySettings_Validation|TestApplyOpenAIFastPolicyToBody|TestApplyOpenAIFastPolicyToWSResponseCreate'
pnpm --dir frontend exec vitest run src/utils/__tests__/openaiFastPolicyUsers.spec.ts src/utils/__tests__/openaiFastPolicyAccounts.spec.ts
pnpm --dir frontend run typecheck
git diff --check
```

**同步官方后的复查：**

- 搜索 `account_allowlist`、`openai_account_allowlist`、`openAIFastPolicyAllowlisted`、`WithOpenAIFastPolicyUserID`、`openaiFastPolicyForm.rules`。
- 如果官方重构 OpenAI fast/flex 策略，需要保留：用户搜索选择多用户放行、OpenAI 账号搜索选择多账号放行、旧配置无字段时行为不变、放行只在该规则 scope + service_tier 已匹配后生效、WS 与 HTTP 共用同一评估逻辑、两个列表都配置时必须同时命中。

## 2026-07-07: 列表筛选状态同步到地址栏

**本地补丁：**

- 新增 `useRouteQuerySync`，统一处理筛选字段从 query 回填、同步到 query、清理普通空值/默认值、保留无关 query；真实页面自动使用当前 Vue Router，局部单测没有 router 时才回退到 `history.replaceState`。
- 搜索框、状态/平台/类型/分组/用户/日期等筛选变化后写入地址栏；刷新页面时先恢复筛选状态并立即同步一次默认/全部筛选参数，再执行原有列表加载；部分原本直接 `load...` 的下拉筛选改为先回到第一页再搜索。
- 下拉框仍以原有默认值为准：账号管理、订阅管理原来默认 `active` 的状态会写成 `status=active`；用户切到“全部状态/全部平台/全部类型/全部分组”等条件时，才在 URL 中写成 `all` token，例如账号管理 `status=all`、`platform=all`、`type=all`、`group=all`；用量、风控等 `null/0` 表示“全部”的字段也用 `group_id=all` 这类 token 回填。
- 接入账号、用户、密钥、用户用量、管理用量、代理、公告、优惠码、兑换码、渠道、分组、订阅、用户订单、管理订单、可用渠道、渠道监控、风控日志、运维系统日志、推广记录。
- 组件单测没有注入 Vue Router 时，工具从当前 URL/history 兜底，不要求每个旧测试额外挂 router；真实页面仍会通过 Vue Router 更新 route query。
- 审查时补充多同步实例回归：同一页面里父页面筛选和子组件筛选先后写 URL 时，后一次同步基于当前地址栏，避免清掉前一次刚写入的 query；补充显式 all/default token 回归，确保默认 `status=active` 能写入 URL，手动选择 `status=all` 时能在刷新后还原为组件内部的全部状态值；补齐部分页面首次加载只恢复不同步的问题，避免默认筛选要等用户交互后才出现在地址栏。
- 账号管理刷新后当前页用量窗口补充强制重拉：`reload()`、顶部手动刷新、待同步列表刷新在表格完成渲染后递增 `usageManualRefreshToken`，让 `AccountUsageCell` 绕过 5 分钟缓存重新请求 `/usage`；自动轮询仍只刷新列表运行态和今日统计，避免高频触发每行用量请求。
- 审查修复账号管理列配置重复 `id` key：保留最前面的专用 ID 列，删除名称列后的重复 ID 列，并补充“列 key 不重复”回归测试。
- 审查修复用户管理已显示筛选变更后未回到第一页的问题：`applyFilter()` 先重置 `pagination.page = 1`，避免在旧页码上请求新筛选结果；补充从第 3 页切状态筛选后请求第 1 页的回归测试。
- `useRouteQuerySync` 补充 nullable 数值筛选的 `all` token 回填测试，防止用量页这类 `group_id=all` 回填成错误类型。

**涉及文件：**

- `frontend/src/composables/useRouteQuerySync.ts`
- `frontend/src/composables/__tests__/useRouteQuerySync.spec.ts`
- `frontend/src/views/admin/AccountsView.vue`
- `frontend/src/views/admin/__tests__/AccountsView.manualRefreshUsage.spec.ts`
- `frontend/src/views/admin/__tests__/AccountsView.usageWindowsHint.spec.ts`
- `frontend/src/views/admin/UsersView.vue`
- `frontend/src/views/admin/__tests__/UsersView.spec.ts`
- `frontend/src/views/admin/UsageView.vue`
- `frontend/src/views/admin/ProxiesView.vue`
- `frontend/src/views/admin/PromoCodesView.vue`
- `frontend/src/views/admin/AnnouncementsView.vue`
- `frontend/src/views/admin/RedeemView.vue`
- `frontend/src/views/admin/ChannelsView.vue`
- `frontend/src/views/admin/GroupsView.vue`
- `frontend/src/views/admin/SubscriptionsView.vue`
- `frontend/src/views/admin/orders/AdminOrdersView.vue`
- `frontend/src/views/admin/ChannelMonitorView.vue`
- `frontend/src/views/admin/RiskControlView.vue`
- `frontend/src/views/admin/ops/components/OpsSystemLogTable.vue`
- `frontend/src/views/admin/affiliates/AdminAffiliateRecordsTable.vue`
- `frontend/src/views/user/KeysView.vue`
- `frontend/src/views/user/UsageView.vue`
- `frontend/src/views/user/UserOrdersView.vue`
- `frontend/src/views/user/AvailableChannelsView.vue`

**验证：**

```bash
pnpm --dir frontend exec vitest run src/composables/__tests__/useRouteQuerySync.spec.ts src/views/user/__tests__/KeysView.spec.ts src/views/user/__tests__/UsageView.spec.ts src/views/admin/__tests__/UsageView.spec.ts src/views/admin/__tests__/UsersView.spec.ts
pnpm --dir frontend exec vitest run src/views/admin/__tests__/AccountsView.manualRefreshUsage.spec.ts
pnpm --dir frontend exec vitest run src/views/admin/__tests__/AccountsView.usageWindowsHint.spec.ts
pnpm --dir frontend run typecheck
pnpm --dir frontend run build
git diff --check
```

**同步官方后的复查：**

- 搜索 `useRouteQuerySync`、`restoreFromRoute`、`syncToRoute`、`RouteQuerySyncField`。
- 如果官方改造列表筛选栏或统一路由 query 管理，需要保留：刷新后能回填筛选并自动重新查询；首次进入也会同步默认/全部筛选参数；筛选变化会更新地址栏；普通空搜索不写入 URL；原有非空默认筛选显式写原默认值；“全部”下拉筛选显式写 `all` token 且不传给后端；无关 query 不被清掉；多个筛选组件不互相覆盖 query；旧单测或局部组件没有 router 注入时不崩溃；账号管理列 key 不重复；用户管理筛选变更回到第一页。
- 账号管理刷新链路还要复查 `refreshCurrentPageUsageCells`、`usageManualRefreshToken`、`AccountUsageCell.manualRefreshToken`：用户显式刷新/批量操作后的 `reload()` 要重拉当前页 `/usage`，但自动轮询不要触发每行 usage 请求。

## 2026-07-11: 合并上游拆分结构后的非 fork 改动落位

**本地补丁：**

- 后端按上游拆分后的文件继续落位本地功能，不恢复旧 monolith：
  - `admin_service.go` 的分组、用户、代理、设置等逻辑继续跟随上游拆到 `admin_group.go`、`admin_user.go`、`admin_proxy.go`、`setting_*.go`。
  - `gateway_service.go`、`openai_gateway_service.go`、`openai_ws_forwarder.go` 继续跟随上游拆分到 `gateway_*`、`openai_gateway_*`、`openai_ws_forwarder_*` 文件。
  - `usage_log_repo.go` 继续跟随上游拆成 insert/query/stats/trend/dashboard，并在新拆分文件里补回 `presentation_multiplier` 的插入、查询和展示统计倍率。
- 前端分组管理继续使用 `GroupSharedConfigSections.vue` 作为图片计费、高峰倍率、支持 scope、MCP XML、Claude Code 限制等共享配置唯一入口；上游新增的 Grok 视频计费迁入该共享组件，并由 `GroupsView.vue` 只传入 form 与最终价格预览，避免创建/编辑弹窗重复渲染。
- 设置页 OpenAI Fast/Flex 策略保留用户放行列表 `account_allowlist` 与 OpenAI 上游账号放行列表 `openai_account_allowlist`；导入的 allowlist helper 改用更明确别名，避免和本地“规则适用用户 ID”添加函数同名。
- OpenAI Fast/Flex 后端保存逻辑保持 `account_allowlist` = 用户 ID 放行、`openai_account_allowlist` = OpenAI 上游账号 ID 放行；读取旧 JSON 时兼容历史只有 `account_allowlist` 的配置，保存时写入空数组作为新 schema 标记，避免刷新后 OpenAI 账号标签丢失。
- OpenAI / Claude 网关响应处理按拆分后的文件落位：`gateway_upstream_response.go`、`gateway_anthropic_passthrough.go`、`openai_gateway_response_handling.go`、`openai_gateway_passthrough.go` 只重写写给客户端的 usage 展示值，内部返回给计费/统计的 raw usage 不乘展示倍率；`image_output_tokens` 同步进入 Claude SSE / 非流式、OpenAI response/chat usage 解析和展示倍率阈值。
- Claude `GatewayService.RecordUsage` 与 OpenAI `OpenAIGatewayService.RecordUsage` 都在请求落库时快照 `presentation_multiplier`，图片输出 token 达到阈值时也能触发；超级管理员 raw 视图仍读取原始 token，不被展示倍率污染。
- OpenAI `buildUpstreamRequest` 对 nil `gin.Context` 做保护，避免测试/内部路径 panic；OpenAI model router 的 `WithModelRouteRequestContext` 迁回拆分后的 `openai_model_router.go`，handler 仍能把 session、请求模型、图片意图、复杂输入特征传给调度层。
- public settings SSR 注入结构 `PublicSettingsInjectionPayload` 补回 `ops_host_health_visible`，保证前端刷新首屏时运维主机健康卡片开关不会先被当作未开启。
- unit-tag 测试按当前拆分签名同步：Claude 流式展示倍率测试通过 `api_key.Group` 注入分组，Grok 视频生成重复测试保留覆盖默认视频时长的一份，订阅仓库 fake 按上游新的 usage window reset 接口签名更新。
- i18n 继续使用上游拆分后的 `frontend/src/i18n/locales/{en,zh}/admin/*.ts` 结构；Fast/Flex 放行文案补在 split settings 文件里，不恢复旧 `frontend/src/i18n/locales/en.ts` / `zh.ts` 单体。
- 合并上游 split i18n 后，旧单体翻译中仍被当前生产代码静态引用的非 fork 文案必须落回新版模块：账号 Spark 使用量/批量令牌刷新/OAuth、公告邮件推送、订阅覆盖确认/批量分配/配额重置、分组展示倍率/订阅总量上限、运维宿主机健康/磁盘/运行阈值、邮件默认语言/订阅过期管理员提醒、公共请求/文件/主题错误等键都按当前 split owner 补回，不恢复旧单体。
- 新增 `localesStaticRefs.spec.ts`，扫描生产源码中的完整静态 i18n key，要求中英文 locale 都存在；动态前缀（如 `status.`、`payment.admin.`）不作为完整 key 检查，避免上游拆分合并时再次静默显示原始 key。
- `localesStaticRefs.spec.ts` 同时校验中英文 split locale 的全部叶子 key 双向一致；本次补回账号表单/反馈、仪表盘、Claude Max 用量模拟、退款放行等缺口，并确认 `admin.accounts.usageWindow.openaiCodexSparkShow` / `openaiCodexSparkHide` 已落在两种语言的 `admin/accounts.ts`。
- 复查发现上游保留了 `BatchImageGuideView.vue` 中的三个 `window.confirm`；统一改用全局 `useAppDialog`，并新增源码扫描测试，禁止生产前端重新出现裸 `alert` / `confirm` / `prompt` 或 `window.*` 原生弹窗调用。
- 主 API 与 Google/Gemini 订阅鉴权只在当前校验没有限额错误时才执行 usage window 维护。总量、日/周/月限额已经明确耗尽时，维护回读失败不能覆盖原本应返回给客户端的 `429`。
- KeysView 的排序回归测试按上游 `SearchSuggestInput` 的新组件名和带关键词的 `search` 事件更新，继续覆盖搜索、分组、状态、页大小和并发排序参数不会互相丢失。
- UsageView 的图表测试先等待异步路由搜索条件恢复，再推进 120ms 假定时器；测试时序与当前 `initializeUsageView()` 链路一致，避免把尚未创建的图表定时器误判为生产代码未请求快照。
- `service.User.IsAdmin()` 重新包含 `super_admin`，并补回 `IsSuperAdmin()` helper：前端、`AdminOnly`、展示 raw-view 和服务层管理员鉴权的角色边界保持一致，避免 super_admin 在管理端请求中被误判为非管理员。
- `wire_gen.go` 不手工合并，待 provider 源文件落定后通过 `go generate ./cmd/server` 重新生成。

## 2026-07-23: 管理端使用记录显示用户备注

**非 fork 需求：**

- 管理员在使用记录明细的“用户”列中，除邮箱和用户 ID 外还要看到该用户的管理员备注；备注非空时显示在邮箱下方。普通用户使用记录接口不得返回该内部字段。
- 管理端 DTO 使用 `AdminUser` 覆盖嵌入的普通 `User`，让管理员列表响应保留 `users.notes`；前端 `AdminUsageLog` 对应使用管理员用户类型。用户排行已有的备注显示保持不变。

**涉及文件：**

- `backend/internal/handler/dto/types.go`
- `backend/internal/handler/dto/mappers.go`
- `backend/internal/handler/dto/mappers_usage_test.go`
- `frontend/src/types/index.ts`
- `frontend/src/components/admin/usage/UsageTable.vue`
- `frontend/src/components/admin/usage/__tests__/UsageTable.spec.ts`
- `frontend/src/main.ts`
- `backend/internal/server/routes/gateway_test.go`
- `backend/internal/service/ops_scheduled_report_service_test.go`
- `tools/secret_scan.py`
- `skills/fork-sync-deploy/scripts/sync-main-into-subapi.sh`

**验证：**

```bash
cd backend && go test ./internal/handler/dto -run TestUsageLogFromServiceAdmin_IncludesUserNotesOnlyForAdmin -count=1
pnpm --dir frontend exec vitest run src/components/admin/usage/__tests__/UsageTable.spec.ts src/components/admin/usage/__tests__/UserTokenRanking.spec.ts
pnpm --dir frontend run typecheck
git diff --check
```

**同步官方后的复查：**

- 搜索 `UsageLogFromServiceAdminWithViewer`、`AdminUsageLog`、`UserFromServiceAdmin`、`UsageTable.vue` 和 `UserTokenRanking.vue`。
- 管理员 `/admin/usage` 与 `/admin/dashboard/user-breakdown` 必须返回并显示非空 `users.notes`；普通 `/usage` 响应不得包含 `notes`。上游调整 usage DTO 或用户脱敏边界时，保留该管理员可见、用户不可见的分离。
- `fork-sync-deploy` 的两次复查报告必须包含 `docs/fork-maintenance/*.md` 中的条目，不能只检查主文档的恢复清单。
- 每次上游合并后必须执行前端 typecheck/build；当 fork 与上游在入口文件采用不同主题初始化结构时，不能遗留旧函数的花括号。
- 上游改变路由构造函数或共享测试辅助方法时，必须编译完整后端测试包；只跑当前功能的窄测试不能发现遗漏的 stub 参数或旧 helper 调用。
- `make secret-scan` 必须可运行；对已在上游受版本控制的 OAuth 客户端常量仅使用精确 SHA-256 指纹例外，绝不能按文件路径或通用前缀放宽扫描。

## 2026-07-16: 账号管理列显示默认项调整

**非 fork 需求：**

- 账号管理“更多操作”中的列显示默认勾选“今日统计”，默认取消勾选“最近使用”“创建时间”“过期时间”；原有代理、备注、优先级、调度分数和计费倍率的默认隐藏状态保持不变。
- 仅迁移未自定义的旧默认列布局；已有自定义列布局继续保留，管理员仍可在同一菜单中恢复这些列。

**涉及文件：**

- `frontend/src/views/admin/AccountsView.vue`
- `frontend/src/views/admin/__tests__/AccountsView.bulkEdit.spec.ts`
- `docs/fork-maintenance/2026-07.md`

**验证：**

```bash
pnpm --dir frontend exec vitest run src/views/admin/__tests__/AccountsView.bulkEdit.spec.ts
pnpm --dir frontend run typecheck
git diff --check
```

**涉及文件：**

- `backend/internal/repository/usage_log_repo_insert.go`
- `backend/internal/repository/usage_log_repo_query.go`
- `backend/internal/service/gateway_upstream_response.go`
- `backend/internal/service/gateway_anthropic_passthrough.go`
- `backend/internal/service/gateway_usage_billing.go`
- `backend/internal/service/openai_gateway_response_handling.go`
- `backend/internal/service/openai_gateway_passthrough.go`
- `backend/internal/service/openai_gateway_forward.go`
- `backend/internal/service/openai_model_router.go`
- `backend/internal/service/setting_public.go`
- `backend/internal/service/usage_presentation.go`
- `backend/internal/service/user.go`
- `backend/internal/server/middleware/api_key_auth.go`
- `backend/internal/server/middleware/api_key_auth_google.go`
- `backend/internal/handler/openai_chat_completions_billing_fallback_test.go`
- `backend/cmd/server/wire_gen.go`
- `frontend/src/views/admin/GroupsView.vue`
- `frontend/src/views/admin/GroupSharedConfigSections.vue`
- `frontend/src/views/admin/__tests__/GroupsView.singleRenderControls.spec.ts`
- `frontend/src/views/admin/SettingsView.vue`
- `frontend/src/i18n/locales/en/admin/settings.ts`
- `frontend/src/i18n/locales/zh/admin/settings.ts`
- `frontend/src/i18n/locales/en/admin/accounts.ts`
- `frontend/src/i18n/locales/zh/admin/accounts.ts`
- `frontend/src/i18n/locales/en/admin/channels.ts`
- `frontend/src/i18n/locales/zh/admin/channels.ts`
- `frontend/src/i18n/locales/en/admin/overview.ts`
- `frontend/src/i18n/locales/zh/admin/overview.ts`
- `frontend/src/i18n/locales/en/admin/resources.ts`
- `frontend/src/i18n/locales/zh/admin/resources.ts`
- `frontend/src/i18n/locales/en/admin/ops.ts`
- `frontend/src/i18n/locales/zh/admin/ops.ts`
- `frontend/src/i18n/locales/en/common.ts`
- `frontend/src/i18n/locales/zh/common.ts`
- `frontend/src/i18n/locales/en/dashboard.ts`
- `frontend/src/i18n/locales/zh/dashboard.ts`
- `frontend/src/i18n/locales/en/landing.ts`
- `frontend/src/i18n/locales/zh/landing.ts`
- `frontend/src/i18n/locales/en/misc.ts`
- `frontend/src/i18n/locales/zh/misc.ts`
- `frontend/src/i18n/__tests__/localesStaticRefs.spec.ts`
- `frontend/src/composables/__tests__/nativeDialogs.spec.ts`
- `frontend/src/views/user/BatchImageGuideView.vue`
- `frontend/src/views/user/__tests__/KeysView.spec.ts`
- `frontend/src/views/admin/__tests__/UsageView.spec.ts`
- `docs/fork-maintenance/2026-07.md`

**验证：**

```bash
cd backend && go generate ./cmd/server
cd backend && go test ./cmd/server ./internal/service ./internal/handler ./internal/repository ./ent/... -count=1
cd backend && go test -tags unit ./internal/service -run 'HandleStreamingResponse_RewritesClientUsageForPresentation|HandleStreamingResponse_RewritesSplitUsageAfterAccumulatedThreshold|ParseSSEUsage|ImageOutput|ForwardGrokMediaVideoGenerationPreservesImageToVideoModel|GetPublicSettings_ExposesHostHealthVisibilityFromEnvironmentConfig' -count=1
pnpm --dir frontend exec vitest run src/views/admin/__tests__/GroupsView.singleRenderControls.spec.ts src/views/admin/__tests__/groupsImagePricing.spec.ts src/views/admin/__tests__/SettingsView.spec.ts src/i18n/__tests__/localesNoKeyCollision.spec.ts
pnpm --dir frontend exec vitest run src/i18n/__tests__/localesNoKeyCollision.spec.ts src/i18n/__tests__/localesStaticRefs.spec.ts
pnpm --dir frontend exec vitest run src/composables/__tests__/nativeDialogs.spec.ts src/views/user/__tests__/KeysView.spec.ts
cd backend && go test ./internal/server/middleware -run 'TestApiKeyAuthWithSubscriptionGoogle_(SubscriptionDailyLimitReturns429|SubscriptionTotalLimitExactlyExhaustedReturns429)' -count=1
cd backend && go test -tags unit ./internal/server/middleware -run 'TestUserAdminRoleHelpers|TestSimpleModeBypassesQuotaCheck' -count=1
pnpm --dir frontend run typecheck
pnpm --dir frontend run build
git diff --check
```

**同步官方后的复查：**

- 搜索 `GroupSharedConfigSections`、`video_rate_independent`、`video_price_480p`、`videoPricing`，确认视频计费仍只在共享组件渲染一次，并且创建/编辑表单都能保存和清空价格。
- 搜索 `presentation_multiplier`、`usageLogSelectColumns`、`usageLogInsertArgTypes`、`Get*ForView`，确认用量展示倍率在上游拆分后的 insert/query/stats/trend/dashboard 入口都没有遗漏，且不影响原始计费字段。
- 搜索 `rewriteClaudeUsageForPresentation`、`rewriteOpenAIUsageForPresentation`、`ResolveGatewayResponsePresentationMultiplier`、`image_output_tokens`，确认客户端展示值和内部 raw usage 仍分离，且图片输出 token 能触发展示倍率。
- 搜索 `WithModelRouteRequestContext`、`openAIModelRouteEvalInputFromContext`，确认 model router 上下文注入仍在拆分后的 `openai_model_router.go` 内，而不是丢回旧 service monolith。
- 搜索 `ops_host_health_visible`、`PublicSettingsInjectionPayload`，确认 public settings DTO 和 SSR 注入字段同步，刷新首屏不会丢运维功能开关。
- 搜索 `account_allowlist`、`openai_account_allowlist`、`addOpenAIFastPolicyAllowlistUserID`、`openaiFastPolicyForm.rules`，确认用户放行、OpenAI 账号放行、双条件同时命中逻辑和旧配置兼容都保留。
- 搜索 `useRouteQuerySync`、`user_search`、`filterUserSearchQuery`、`restoreSubscriptionRouteQuery`、`filterUserKeyword`，确认订阅/用量等页面刷新后仍能回填可读搜索文本，不把内部 `user_id` 反写成搜索框内容。
- 搜索 `frontend/src/i18n/locales/en.ts`、`frontend/src/i18n/locales/zh.ts`，确认不会恢复旧单体；运行 `localesStaticRefs.spec.ts`，确认生产源码静态引用的完整 key 在中英文 split locale 中都有落位。
- 运行 locale 叶子键双向一致性校验；不要只补当前页面触发到的 key，新增或迁移 split locale 时中英文任一侧新增的 leaf key 都必须同时落到另一侧。
- 搜索 `window.confirm`、`window.alert`、`window.prompt`，确认生产前端只使用 `useAppDialog` / `AppDialogHost`；批量图片任务取消、单条删除和批量删除也不得退回浏览器原生确认框。
- 搜索 `ValidateAndCheckLimits`、`EnsureWindowMaintenance`、`ErrTotalLimitExceeded`，确认已判定的订阅限额错误仍直接转换为 `429`，不会被窗口维护或数据库回读错误错误改写为 `500`。
- 搜索 `IsAdmin`、`IsSuperAdmin`、`RoleSuperAdmin`，确认管理员接口鉴权、前端管理员布局和用量 raw-view 对 super_admin 的识别一致。
- 如果上游继续拆分、移动或重命名文件/方法，应以新结构为准重新实现本地需求；不要通过恢复旧大文件来“解决”冲突。
