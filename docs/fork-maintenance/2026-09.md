# 2026-09 Maintenance

## 2026-09-23: Explain user quota rejections

- Requirement: identify the exhausted quota, USD limit and recovery duration instead of a generic usage-limit message. Distinguish expired/suspended subscriptions and quotas that cannot reset.
- Implementation: subscription prechecks and billing cache checks share `quota_error_details.go`; platform quota cache/DB paths and API key 5h/1d/7d windows include the enforced limit, usage and known reset timestamp. Existing error reasons/statuses and quota enforcement thresholds remain intact.
- Subscription reset dates use the existing daily/weekly/monthly model helpers. Do not promise recovery when the reset is at or after subscription expiry. Unknown/past reset times explicitly require administrator assistance. Total quota does not reset automatically.
- Chinese/English and default bilingual messages preserve detailed amounts and recovery time through gateway and standard response localization. Cached expired and suspended subscriptions return their specific reasons.
- Verification: `cd backend && go test ./internal/service ./internal/handler ./internal/server/middleware ./internal/pkg/response -count=1`; regression cases in `quota_error_details_test.go`, `response_localization_test.go`, Google middleware and OpenAI billing fallback tests.
- Review: verify actual quota rejection paths retain fallback routing/error identity, use the enforcement window and do not overwrite detailed messages during localization.
- Deployment pending: local Linux amd64 build produced `/tmp/sub2api-quota-details/sub2api`. SSH alias `51tokens` timed out on its configured port during this change. No production environments have been modified. Verify current topology (including 3dbooks) before uploading and rolling out; compile locally only.
